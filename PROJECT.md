# Pocket — Project Reference

The living description of how Pocket is built. Update this whenever a screen, data
model, service, entitlement, build config, or architecture decision changes
(see the doc table in `AGENTS.md`).

## What Pocket is

A native iOS guitar-practice tool that attaches practice data (loops, markers,
notes, session history, routines) to songs in the user's music library. The app
is an intelligence layer over the library — it never replaces it.

**The position, stated once:** Pocket is the intersection of two categories that
normally ship separately — a **session conductor** (routines, blocks, history) and a
**real audio engine over the player's own DRM-free files** (waveform, A/B span,
time-stretch, markers, takes, a ramp against a click). The practice organisers have no
engine; the slow-downers model no session. Holding both is the claim; *"we have
routines"* is not, because nine competitors have those. The multiplier rule above
generalises from the user's **music** to their **learning sources**: we reinforce
whatever they already learn from — a lesson, a teacher, a course — and never ask them to
move it here. Full argument, competitor roster and the surfaces still contradicting it:
**`docs/positioning.md`** (2026-08-16).

- **Platform:** iOS 17+, phone-first, Swift / SwiftUI.
- **Name:** **Red Moon Practice** on the App Store; **Pocket** everywhere in the
  code. The Xcode target, the bundle id and this repo keep the internal name —
  the two are not going to be reconciled, so read "Pocket" below as the codebase
  and "Red Moon" as the product. The paid tier is **Red Moon Pro**.

## Audio sources (decided)

| Source | Role | Why |
|---|---|---|
| Local / iCloud files | **Primary** — full engine (waveform, speed, loops) | DRM-free; AVFoundation can read raw PCM |
| Apple Music | **Not shipped.** Browse / metadata only *if ever built* | DRM blocks raw-audio access; waveform/time-stretch not possible without a special, selectively-granted entitlement |

See `docs/decisions/0001-audio-source-local-first.md`.

⚠ **v1 ships no Apple Music access at all.** There is no MusicKit import, no
`MPMediaLibrary`/`MPMediaPicker`, and `SongRef.appleMusic` is constructed only in
tests — the `.appleMusic` case is a dormant model seam, not a feature. Accordingly
`NSAppleMusicUsageDescription` was **removed** from `Info.plist` during submission
prep: a usage string for an unexercised capability is an over-broad permission
(see `AGENTS.md` ▸ Conventions) and contradicts the App Review note's "never Apple
Music streaming audio". Building the browse path means re-adding the string in the
same commit.

## Architecture (V1)

- **App data (solo):** SwiftData, with CloudKit sync planned (Phase 4). Apple's
  iCloud — **not AWS**.
- **AI planner backend:** a thin proxy that holds the Claude API key. The app
  never holds the key. Base URL is chosen by build config:
  - Debug → local / non-AWS dev proxy (accessible, fast to iterate)
  - Release → small AWS prod (Lambda + API Gateway)
  - See `docs/decisions/0002-ai-proxy-backend.md` and `infrastructure/`.
- **AWS collaboration layer** (shared setlists, DynamoDB/S3) is **parked** — not V1.

## Identity model

Practice data attaches to a `SongRef` (`Pocket/Core/Models/SongRef.swift`), a
stable `(id, source)` identity **designed** to work for both local files and Apple
Music — though only the local-file source is ever constructed at runtime; the
`.appleMusic` case appears solely in `SongRefTests`.

**Resolution is by owned copy first (ADR 0148).** An imported file is copied into
the app's own container (`SongFileStore`) and its leaf recorded on
`Song.audioFileName`; `SongAudioResolver` tries that copy, then the legacy
security-scoped bookmark, then nothing. The bookmark is kept as provenance and as
the pre-0148 fallback — it is **not** part of identity, and it is no longer the
primary path: a bookmark is granted to one *installation*, so a library restored
from a device backup came back whole and played nothing. Neither the copy nor the
bookmark orphans loops/markers, because identity is the `(id, source)` pair alone.

## Modules

| Path | Responsibility |
|---|---|
| `Pocket/App/` | App entry, root scene. Also the two **global device-state** gates, both driven from view modifiers: `KeepAwake` (ADR 0050) and `OrientationGate` (ADR 0042). **`KeepAwake` is reference-counted** (`KeepAwakeLease` + `KeepAwakeClaim`, the same shape as `AudioSessionClaim`): `isIdleTimerDisabled` is one process-wide slot, so writing it straight from a per-screen `onDisappear` meant the outgoing practice surface cleared the hold the incoming one had just taken — which is every block change in a routine. Surfaces now take and release a *claim*; `RoutinePlayerView` holds one for the whole session so the hold spans block changes structurally rather than relying on every block kind opting in. `OrientationGate` has the same single-slot shape and is safe only because exactly one screen (`WaveformPracticeView`) opts into landscape |
| `Pocket/Features/Home/` | Home hub — the app root: greeting, a primary **Start today's session** CTA (→ planner, goal-adaptive), the resume card, then the nav strips grouped into titled **sections** (`HomeSection`, ADR 0102) — **Practice** (Practice · Metronome) and **Your stuff** (Song library · **Journal**, the notes+takes history space — read-only for *owned* entries, but since ADR 0155 it writes **standalone** notes of its own from a ＋ — warm-gold, ADR 0100 · **Toolkit**, the chords/theory reference hub, indigo, ADR 0096). Toolkit sits in "Your stuff" only until Red Moon Oracle ships, when the two split into a **Learn** section (ADR 0102). Then a **Recent routines** rail (last 3 routines *practised*, tap → opens that routine's `RoutineDetailView` (blocks + Edit + Start), not a straight replay — supersedes ADR 0066's one-tap-replay-from-home; the routine library ▶ still replays directly; reads `Routine.lastPracticed`); Settings top-left, add-song **+** top-right (`HomeView` + `HomeCards`, ADR 0044 / planner review R1) |
| `Pocket/Features/Library/` | Song library, file import, song metadata editing. Import is **multi-select** (`SongImportModel` — decodes each file off-main via `Task.detached`, `SongImporter.prepare`/`persist` split, progress overlay + partial-failure summary; good files import even when others fail). **ADR 0148:** `prepare` now also **copies the picked file into the app's own container** (`SongFileStore`) at the one moment it is provably readable, and `Song.audioFileName` records the leaf. The security-scoped bookmark is kept as provenance and legacy fallback, no longer the primary path — a bookmark is granted to one *installation*, so a library restored from a device backup used to come back whole and play nothing. **No demo song is seeded any more** (`SongPresets` deleted, ADR 0148 §7). **`SongRelinker`** (§6) points an *existing* song at a file again — same `prepare`/`apply` split as the importer, keyed on the song's existing `sourceID` so loops/markers/takes/`PracticeRun` history all survive (re-importing would mint a new song and strand them); re-extracts the waveform, and reports a >1s duration change so the player is warned that loops may no longer line up — without deleting any. Offered as **Find the file**, the primary button on `AudioUnavailableNotice`, so the repair sits where the failure is discovered — **and, since ADR 0152, as an always-available *Replace audio file…*** in the new **Audio** section of `SongDetailsSheet` (`SongAudioSection`, its own file since the sheet was at the `type_body_length` cap) — whose *Exercises for this song* section now lives in `SongDetailsSheet+Links.swift` and whose drill rows open the run screen (ADR 0172). That second door exists because §6's "the door is the failure itself" has a blind spot: a relink onto a readable-but-**wrong** file *succeeds*, so the song resolves, `AudioUnavailableNotice` never returns, and the only route back closes — with re-import barred by §4 (new `sourceID` ⇒ stranded loops/takes/history). Details is reachable from the library row menu **and** by holding the title on the practice screen, one gesture from where a wrong track is heard. The replace operation is **injected**: the library uses `SongRelinker.replace` directly, while the practice screen passes `WaveformPracticeModel.relinkAudio`, because `SongFileStore.adopt` rewrites the file the engine currently has **open** and only that caller can stop and reload around it. Confirmation before (irreversible write in a browsing surface), §6's >1s length warning after, and explicit success confirmation (unlike the practice screen, this sheet looks identical afterwards). The file line shows **format + size, never a name** — the copy sits under a `sourceID`-keyed leaf, so the on-disk name isn't the player's (pure `SongAudioLabel`, unit-tested across all three custody states) |
| `Pocket/Features/Waveform/` | Timeline, markers, loop creation (the practice screen). **ADR 0170 — a tempo you can take with you:** holding the speed bar's BPM callout no longer opens the tempo editor (the metronome control beside it is now that editor's only handle) but lifts the **effective** tempo — `song.bpm × speed`, the number on screen — into `CarryTempoSheet`, which offers the metronome (`MetronomeView(initialBPM:)`, seeded through `setBPM` so the song player's wider range is clamped, with the song paused via `pauseForNestedAudio`) or a new exercise (`NewExerciseSheet` seeded with the tempo, the song's meter and — via the new `initialSongs` — an ADR 0111 link back to the song, inserted through the shared `NewExercisePlan.finalise(in:)`). The destination replaces the chooser's content in place, so the screen declares **nine** presentations, not eleven; the tempo is snapshotted per hold with a minted identity (ADR 0160 §5's lesson, ADR 0090's rule from the other side); no new hold was spent, so the `long-press-sites` tripwire stays at 9 while `loop-controls-rows` moves 8 → 9 |
| `Pocket/Features/Metronome/` | Standalone metronome screen (ADR 0043; `MetronomeView(initialBPM:)` opens it already on a carried tempo — ADR 0170's song-player gateway — seeded in `init` because the body reads `engine.bpm` on its first pass, and through `setBPM` because a carried tempo is the first number to reach it from outside its own controls and can fall outside 30–300; automator phase-continuous stepping + explicit run/count-in/infinite, ADRs 0047/0048). The hero BPM readout is **typable** (`TypableTempo`, 2026-08-18 review note): a fourth way in beside ±1, the slider and TAP, and the only one that can name a tempo outside `bpmRange` — it commits on focus loss through the same `setBPM` clamp the steppers use, so an out-of-range entry snaps back rather than being refused, and an armed ramp re-bases on the typed floor. Click voice is user-selectable (Click/Wood block/Rim/Beep) via Settings, applied to this tool and the in-song click alike (ADR 0114). Also the **only** home of **click withdrawal** (ADR 0132, §4 as amended after the device pass): the click thins itself out on a fixed eight-bar cycle — Off/Gentle/Standard/Deep, **default Off** because a metronome that stops clicking is indistinguishable from one that has broken — chosen in the screen's **`MetronomeSettingsSheet`** beside the time signature and subdivision (the nav-bar meter control opens a sheet, not a menu — fifteen options across three groups had made the popup scroll, clip and truncate; the sheet also carries the footer prose click withdrawal can't do without), not in Settings, because it applies here and nowhere else. Suspended while any ramp climbs (a moving tempo and a withdrawing click are two demands at once); the host opts in explicitly via `allowsClickWithdrawal`, so no other screen driving `StandaloneMetronomeEngine` can inherit it. Its toolbar pencil writes a **metronome journal note** (ADR 0160): `QuickJournalButton` pins `engine.journalContext` — BPM, time signature, subdivision and the withdrawal *in force* — at the tap and presents `QuickJournalSheet(owner: .metronome(…))` by item, so a ramp moving behind the sheet can't change what the note says |
| `Pocket/Features/Practice/` | Top-level Practice hub → two unit libraries (`ExerciseLibraryView`, `LoopLibraryView`) plus a **Long-term goals** list (`LongTermGoalListView` — the ranked standing tier's *only* editable surface, ADR 0171; drag to reorder behind a visible toolbar control, capped at ten, authored through `LongTermGoalEditorView` off the **shared** `GoalAuthoringSections` so both goal tiers use one template picker / skill trimmer / target-song row) plus a **Routines** library (`RoutineLibraryView` — ▶ plays, row-tap edits, three curated in-house starter routines seeded once via `RoutinePresets` (after the exercise presets, blocks resolved by name; exercise-only at cold start) — → `RoutineDetailView` editor: name + an optional **Description** (`Routine.notes`, ADR 0177 — what the session is *for*, above ADR 0167's *where you learned it*; both prose fields live in `RoutineDetailView+Prose.swift` and share one gate: edit mode or a provisional generated session, trimmed at the two commit sites, and drawing nothing at all when read-only and empty), add exercise/loop/song blocks via `AddRoutineUnitSheet` (two-level picker: Exercises→template, Loops→song, plus a flat **Songs** bucket, plus an **Ear training** bucket that adds a loop as an ears-only `.ear`-mode block — ADR 0104 Slice 2; loop/ear rows carry a per-row **audio audition** via the reusable `AddRoutineUnitRow` + `LoopAudioPreviewPlayer`, one loop at a time, and the whole sheet is **`.searchable`** across every element — exercises · loops · ear training · songs; **ADR 0127** makes the picker **multi-add**: a tap adds the unit and the sheet *stays open* (checked row, running tally, second tap un-adds — scoped to that picking session), collapsing the four `onPick…` callbacks into one `onToggle: (RoutineUnitPick) -> Void` whose `pickID` is the single row identity the editor keys its `pickID → RoutineItem.uid` map on; drill-in levels split to `AddRoutineUnitLists.swift`; each **grouped** level also carries an **All exercises / All loops / All ear training** row in a section of its own *above* the groups — the same units flat and A→Z, for finding a unit by name rather than by remembering its group; hidden at one group, and the flat exercise rows carry the **template** as their context line since the section header no longer does; Songs is already flat and unchanged), **rest placement** (ADR 0127 — *Insert rest* tapped appends as before, **held** it enters a rest-insert mode replacing the list with `blocks + 1` tappable gap rows; the pure `RoutineBudget.allowsRest(at:in:)`/`restSlots` guard both paths with one rule — **a rest may never sit next to a rest** — and a refused slot answers the tap with an anchored "Rest already here" popover rather than going silently inert; authoring-only, no model change and no migration, so an existing routine with adjacent rests still loads and runs; the entry row is a plain shape with separate tap/long-press gestures, **not** a `Button`, whose action also fires on a long press's release; `RoutineDetailView+Units.swift`/`+Rests.swift`), drag-reorder, and **tap a unit block → a `BlockRepsEditor` sheet** to set its **Repeat ×N** (1–9, ADR 0076; a `×N` badge shows in both modes); and the **player** `RoutinePlayerView` (thin `NavigationStack` host over the `RoutineSessionPlayer` conductor + pure `RoutineSessionCursor`) that **embeds the real `ExerciseRunView`/`LoopRunView`/`EarLoopRunView`/`SongPlayAlongView` per block** (an `.ear`-mode loop block resolves to `RoutineStageKind.earLoop` and embeds `EarLoopRunView` — the shared `EarTrainingView` core + routine chrome + a manual **Done**, no completion screen since ear training grades nothing, ADR 0104 Slice 2) — injected with a `RoutineRunContext` (progress · Skip · natural-completion hook · exit) so every training aid is kept, not re-implemented — though an **exercise** block in a routine no longer mirrors the library editor (ADR 0077, Slice 2): `ExerciseRunView` gates on `routineContext != nil` to drop Save, journal and the meter picker (the pre-run promote button is gone everywhere — promotion is now a post-run offer, ADR 0079), keeping only the collapsible **Practice Settings** panel (tempos + step granularity, incl. the ADR-0078 dwell), the ramp, count-in, live BPM and staircase — edits committed on Start — with a fixed rest countdown between blocks, judgement-free; a finished unit lands on a **Done screen** (`RoutineBlockDoneView` — completion beat + optional mastery tap + optional inline note **with a kind tag** (🎯/⚡️/🧗/📝/🎬, default `.note`, threaded into `JournalWriter.add(kind:)` at all three run hosts — ADR 0100) + an optional **Move command to {value}** promote toggle for a summited exercise (opt-in, exercises-only, absent under auto-advance; the target defaults to the reach but is **editable** via a ±/typed stepper for a custom command — ADR 0079 §7) + an **Up next** card previewing the next unit (rests skipped), committed together on Continue/Finish; a top-left chevron exits the routine from here; collapses the old reflection sheet) with **manual advance the default** (the `routineAutoAdvance` setting, default off, restores auto-advance; a Skip always bypasses the gate — ADR 0071 R4); a **multi-rep block** (ADR 0076) runs back-to-back with a **Rep N of M** counter on the progress strip, the Done screen only after the last rep, while **Skip** jumps past remaining reps to the next block; tapping **Start** runs **straight into block one** (previews happen up front, so the first block no longer waits — ADR 0071 R4b); each block is inspectable before starting via a read-only **block preview** (`RoutineBlockPreview` — pushed on tap: `ExerciseBlockPreview` shows content + an editable collapsible `PracticeSettingsPanel` (tempos + steps + dwell, written straight to the model via bindings — ADR 0077 Slice 2 / 0078) + staircase + a `CommandTempoPreviewPlayer` metronome audition — or, for a strumming / Chords & Strum drill, a `StrumPatternPreviewPlayer` **Hear the strum** rhythm audition (the metronome engine's pattern mode, `StandaloneMetronomeEngine+Strum`, plays the pattern's down/up/accent/mute slots as clicks; rhythm not tone — ADR 0071 R5), `LoopBlockPreview` shows source + speed + an editable `LoopSettingsPanel` in percent-of-original units, written straight to the model as its exercise counterpart is — ADR 0130 §4, closing ADR 0077 §7's parked "loops are out of scope"; ear blocks are excluded, having no ramp — + staircase + a `LoopAudioPreviewPlayer` audition of the loop's real audio; both non-ear previews carry `BlockLengthControl`, the ADR 0130 length note + per-block opt-out, shown only on a block a session allotted minutes to); a **song block** runs the audio-only `SongPlayAlongView` (+ `SongPlayAlongModel`, own `PracticeAudioEngine`): fixed play-along speed (no ramp) · play/pause · −10s/+10s, looping-until-skip by default or play-through-once-and-advance per the `routineSongLoop` setting; ADR 0066 slices 2–3 / ADR 0071); template-first exercise **creation** (`ExerciseTemplatePicker` → `NewExerciseSheet` configure step, whose **two hosts** — this library's `+` and the metronome automator's "Save as exercise" seam — share **one insert**, `NewExercisePlan.finalise(in:)` (ADR 0128: payload encode → rhythm bind → `insert` → song attach → analytics; hung off the *plan* because the preset seeder shares the `commandAnchored` factory but never builds a plan); ADR 0068 — picker order + retired Fingerstyle/Rhythm + Chords & Strum rename, ADR 0087; the ex-"Coming Soon" **Ear Training/Theory** rows removed 2026-07-22 — ear training shipped as a loop mode, ADR 0104, and the enum cases live on only for the planner's `SkillFamilyMap`); per-unit training-run screens (`ExerciseRunView` / `LoopRunView` + `LoopRunModel`) — a standalone exercise **or loop** run that **finishes naturally** presents the **same `RoutineBlockDoneView`** a routine block finishes on (`upNext: nil`, ADR 0079 §2 revised; loops brought to parity by ADR 0082, at a 200%-of-original ceiling) — completion beat + optional mastery + note + an editable **Move command to {value}** promote toggle (defaults to the reach, ±/typed for a custom command) that persists immediately on Finish (or no toggle when there's nothing above command); pure `PromoteOffer` math (ceiling-aware `canPromote`); the old pre-run promote button is gone for both, an exercise **detail/reference sheet** (`ExerciseDetailSheet` — a **purely read-only reference** sheet (ADR 0077, reordered description-first, **tempo editing removed** — command/reach are tuned on the run screen / in-routine block surface instead — and, Slice 3, **content/shape editing removed** too): editable **description** + self-rated **mastery** + read-only meter (**Feel** — time signature + the played **Rhythm** (`Exercise.noteRate`, omitted when the drill states none; the never-wired **Subdivision** row and field were retired by ADR 0121)) + a compact read-only **template** chip at the bottom (ADR 0068), committing only notes + mastery — the redundant routine-staircase preview was dropped (device feedback, it lives on the run screen); ⓘ from the run screen); the exercise **shape editor** (`ExerciseShapeSheet` — ADR 0077 Slice 3: the per-template content editor relocated off ⓘ onto a compact **Edit shape** control in the board preview's header on the library run screen (`routineContext == nil` only — in a routine an exercise is tempo-only), hosting `StrumPatternEditor` for strumming, `FretboardRunEditor` (declare a movable finger-pattern run + live preview) for the warm-up families, a **generate-or-draw** split for Scales — `ScaleRunEditor` (pick scale + root + position + octaves; generated box + live preview) **or** "Draw your own" → the `FretboardDrillEditor` with a **scale guide** ghosting a chosen `ScaleReference` scale+key so you tap out symmetric/exotic/hand-shaped runs the box generator can't produce (ADR 0107), `ArpeggioRunEditor` (pick quality + root + position + octaves) for Arpeggios, `ChordProgressionEditor` (build a progression of chord voicings + per-chord beat holds, **reorderable since ADR 0172** via per-row up/down arrows over the pure `ChordProgression.movingChange(at:by:)` — arrows rather than a drag because the rows are a `VStack` inside a *single* `Form` row, which `.onMove` cannot reach; all five pure editors now rebuild through one private `with(changes:)` that carries `keyRoot`/`keyIsMinor` across, fixing a long-standing silent reset of a progression's declared key — each Add/swap opens the **search-first `ChordPickerSheet`** (ADR 0103): a live search field over an **Insert** grid of mini diagrams in **collapsible sections** (tap a header to fold it) grouped My chords → Movable shapes (tap → root menu → placed grip) → **Triads** (root-position maj/min on the G‑B‑e / D‑G‑B / A‑D‑G string sets, same root-menu placement — ADR 0109) → Open shapes, plus a **Build** segment carrying the Movable/Custom authoring sheets; on a **bass** drill every guitar section stands down and a single *Bass shapes* section takes their place — the `BassChordShape` dyads and shells, same root-menu placement, with My chords filtered to the neck being written for, ADR 0164) for Chords, `StrumPatternEditor` + `ChordProgressionEditor` stacked for Chords & Strum (a `StrumChordSheet` pairing the two, no reset coupling between them), and `FretboardDrillEditor` (tap-to-place notes, split across `+Board`/`+Guide`) as the custom escape hatch — ADR 0065; a **Rhythm change that would revalue a measured command tempo** is intercepted on Done by `RhythmChangePrompt` (two answers, no cancel — *keep the same note speed*, rescaling every tempo so notes-per-minute holds, or *re-measure*, clearing the command; nothing is written until one is chosen — ADR 0121); **fretboard authoring polish, 2026-07-28**: shared chrome in `FretboardEditorChrome` — one named `EditorDisclosure` (Advanced holds Rhythm · Octaves · Sequence · Up-and-back · Start-from-lowest-root, with a deviations-only summary; the picking editor's *Movement* keeps its own name), `RhythmRow` (ex-Subdivision, now a dropdown), chevron `EditorStepper` with a Futura sentence-capitalised position label, and `FretLabelMode.available(hasRoot:)`/`.resolved(hasRoot:)` hiding Interval captions on rootless surfaces; the draw-your-own board and `ChordBoardEditor` both reach fret 24 with shared `FretboardGrid` inlays, the step strip pulses its dot on the neck (+ a count pip for a reused note, + a walk connector trail in `FretboardGrid`), and Undo is a ⌫ that returns the cursor to the step it restored; the one-shot **Watch** preview (`FretboardPlayOnceButton`) now hides when "Animate exercises" is on and shows when animation is off / Reduce Motion (ADR 0077, reversing 0065), and the dead "Sound soon" scaffold button was dropped while its `ExerciseAudioEngine` seam is kept); the content-template renderers (`StrumLane` + live `StrummingLaneView`; `FretboardGrid` + live `FretboardView` + self-driving `FretboardDrillPreview`, ADR 0065 build 2 — with the ADR-0083 S5 **following viewport**: a climb wider than the comfortable board (~8 frets) shows, while walking, a hand-width window (pure `FretboardDrill.displayWindow(activeIndex:)`) that follows the sounding note by only scrolling when it reaches the edge (hysteresis; the note lands near the trailing edge to prioritise the runway ahead, so it reframes only ~twice over a whole climb) and hides off-window notes, never reframing while the note is visible; a gentler climb or short run keeps the static full-neck reference diagram; plus ADR-0083 S2b **pass focus** — while a multi-pass climb walks, off-pass notes fade to a ghost so the active position reads clearly (driven by a transient parallel `FretboardDrill.noteGroups` filled at generation, excluded from `Codable`; single-pass runs and the static board are unaffected); `ChordDiagramView` + live `ChordChangeView`, ADR 0065; `StrumChordsView`, the chord surface stacked over the strum lane on one shared beat origin); the library grouped into **template sections** (`PracticeLibrarySort.exerciseSections`, ADR 0068); a collapsible **Practice Settings** panel (`PracticeSettingsPanel`) grouping the run-setup tempos + Steps + curated starter exercises — a fresh install seeds the **six-exercise first-run set** (`PracticePresets.firstRunSlugs`, ADR 0112: ADR 0112's union of the four one-time freebies and the four drills Morning Routine needs, kept intact as ADR 0144 **trial content**, so everything a new player sees is runnable); the wider catalog below is **retired from seeding but kept** as the table the provenance back-fill matches an older install against, and existing players keep everything they were already given — — six v1 technique drills + a v2 strumming preset + a v3 fretboard warm-up + a v4 scale run + a v5 arpeggio + a v6 chord progression + a v7 accent/mute strumming preset + a v8 strum & chords groove (`PracticePresets`, ADR 0046/0065); and the **V2 planner UI** (Slice 3, ADR 0015/0072/0073): `PlannerView` (the "Today's session" entry — duration selector, goals list, **Generate** → provisional `Routine` review with an inline editable **Name** field + an **Estimated length** readout and soft over/under-budget hint vs. the chosen length (R3, `RoutineDetailView+Length` supplies the values; **ADR 0173** moved the *section* into `RoutineDetailView+History`, which folds the estimate together with the routine's own history — last practised + how many times, read off `PracticeRun.routineUID` via the pure `PracticeLog.routineHistory`. One section rather than two because the old estimate gated on `!existsInStore` and a history can only exist on a routine that *is* stored, so two sections would have shown one or the other and never both — and the estimate, the single most useful thing to choose a routine by, was hidden on exactly the routines you'd be choosing between. The count is **sittings, not rows**: the log writes one row per completed block, so counting rows would report a six-block routine's single morning as six practices. Facts only — no streak, no denominator, no delta; a never-run routine says *Not yet*. The **library row** carries the tally too, beside its block count, via the one-pass `PracticeLog.routineSessionCounts` reduced **once per redraw** in `body` and handed down — a per-row read would rescan the whole log for every routine on screen. There the never-run case shows *nothing* rather than *Not yet*: a list of thirty rows each naming a thing not done reads as a nag, so unrun routines are absent from the map rather than present with a zero) → shipped player; no-goals falls back to a Quick session) and `GoalEditorView` (template picker → name → priority → skill-trim + an **Add skills** full-catalog search (`SkillPickerSheet` — a `.searchable`, family-grouped picker over the whole `TechniqueTaxonomy`, R2) → optional target song → met/delete, writing `Goal`s) |
| `Pocket/Core/Planner/` | **V2 practice planner** (ADR 0072/0073/0074, Slices 1–4) — pure ranking + layout, Foundation-only. **Back-half (S1):** `PlannerCandidate`/`SessionBlock` (value projections), `DueScore` (the `goalWeight × dueness × (1−mastery/5)` selection ranking), `SessionBuilder.buildSession` (ranked candidates → U-shape session honouring ADR 0014 pacing) + warm-up LRU pick. **ADR 0129 — a preset counts *blocks*, not minutes:** `SessionLength` resolves to `blocks × itemsPerBlock` (Quick 1×3 · Focused 2×3 · Full 4×3), each block 15 min shared among the items it actually holds, a rest **between blocks** not between items, and R7's hour reached exactly by `.full`. A candidate's own `estimatedMinutes` no longer sizes its slot — the block's share does, and the run fits its ramp to that share (`SessionEstimate.fitted`, dwell-only stretch) via the new `RoutineItem.plannedMinutes` → `RoutineStage` → `RoutineRunContext`; the stored recipe is never rewritten. **Amended after the 2026-07-31 device pass:** the fit is *bounded* — `SessionEstimate.clampedDwell` holds it within 0.5…2.5× the authored dwell, and where the clamp bites the block's estimate gives way (`effectiveMinutes` prices the ramp that will actually play, so a readout never promises time the run won't spend); **loops fit the same way** through the sibling `LoopEstimate` (a loop ramp's intervals are *passes* and its plateaus *percent of original*, so it needs its own estimator) with `Loop.rampFloor` giving an un-measured loop a real warm-up; and `SessionBuilder` **deals across goals** (`PlannerCandidate.goalUID` → `roundRobin`) so a three-item Quick session can't be drawn entirely from one goal. Minutes everywhere are now a derived **total-time estimate**, one meaning in both builders. **ADR 0130 — a block may decline the fit:** `RoutineItem.usesAuthoredLength` (additive `Bool = false`) is a per-block opt-out — a flag rather than a cleared `plannedMinutes`, which would destroy the allotment and make the control one-way — read everywhere through the single `RoutineItem.effectivePlannedMinutes` (`usesAuthoredLength ? nil : plannedMinutes`) so the run, the block preview and `estimatedMinutes(forRoutine:)` cannot disagree; the block preview names both numbers where they differ and carries the toggle (`BlockLengthControl`), and the routine's estimate re-flows immediately. **Front-half (S2):** `TechniqueTaxonomy` (pure skill table — id/difficulty/mode/prereqs) + `SkillFamilyMap` (`ExerciseTemplate → [SkillID]`, coarse) + `CandidateDeriver.deriveCandidates` (active `Goal`s → ranked candidates: Path A technique→exercise via the family map, Path B repertoire→target-song loops+run, soft direct-prereq down-weight) + `GoalTemplateLibrary` (4 curated goal templates) + `PlannerLibrary`/`PlannerGoal` projections + `PlannerID` (deterministic song id). **Session length (R3):** `SessionEstimate` — pure ramp-staircase → minutes (per-plateau tempo × meter), a `reps` multiplier, and a soft-budget `fit` (under/onTarget/over vs. the chosen length); `PracticePlanner.estimatedMinutes(forRoutine:)` sums it across a routine's blocks. `RoutineItem`'s additive `reps: Int = 1` is now **authored** (editor stepper) and **looped** by the player (ADR 0076): the pure `RoutineSessionCursor` steps reps within a block before advancing, `RoutineStage` carries `reps`. **UI (S3):** `GoalPriority` (pure Low/Normal/High ↔ `weight` bridge). **Skill catalog (R2):** `SkillCatalog` + `SkillFamily` — pure display grouping (family by `SkillID` prefix) + case/diacritic-insensitive name search over `TechniqueTaxonomy`, backing the goal editor's full-catalog skill picker. **Loop skill-tags (S4, ADR 0074):** `SkillFamilyMap.recognizedTemplate(for:)`/`taggableTemplates`/`suggestedLoopTags` recognise a `Loop.tag` as a coarse `ExerciseTemplate` bucket, projected onto `PlannerLoop.templates` so Path A also surfaces tagged loops (untagged = Path B only; reuses ADR 0034 tags, no schema change). **Run mode reaches the plan (ADR 0135 S3 + 0139 S1, built adjacently):** a `LoopRunMode` now survives candidate → block → item — `PlannerCandidate.runMode` → `SessionBlock.focus/play(…, mode:)` → `RoutineItem.loopRunMode`, set by the materialiser on **loop** blocks only — without which a planned ear/improvise block persists as the *trainer* and hands the player a ramp neither mode requires; deliberately **not** on `PlannerUnitRef`, which is the dedup key (a mode-bearing key would let one loop appear twice, once to train and once to sing back). Loops now reach a skill three ways: by **tag** (0074), by **flag** (`isBackingTrack`, 0135) and by **capability** — `SkillFamilyMap.directLoopModes` resolves the three `ear.*` skills to any audible loop and `improv.vocabulary` to flagged ones, both closing permanent **zero-candidate holes** (`ear.*` mapped to the non-`creatable` `earTraining` template; `improv.vocabulary` is `.repertoire`, whose Path B returned `[]` for a goal naming no target song, which "Improvise in a style" deliberately doesn't). Which loops qualify is left to `LoopModeAccess` (ADR 0138) via `PlannerLoop.modeFacts`, so the planner can never schedule a mode a run screen would refuse. A jam plans as the trailing **`play`** block (0135 B6a), partitioned out *before* selection so it costs no focus item, one per session, only when the preset schedules a play-through, priced by `RampLessBlockLength` (ADR 0141) since a play block carries no `plannedMinutes`. **`SessionConstraint`** (0139) is a defaulted parameter on both entry points, not a second planner: under `.offGuitar` everything needing the instrument is dropped (exercises, song runs, the warm-up) and surviving loops are **pinned** to `.ear` rather than filtered — pinning is what lets a song-targeted goal contribute at all. Surfaced as an unpersisted **Away from your instrument** toggle on `PlannerView` (never "off-guitar" — ADR 0116), naming its result a *Listening Session* with a loops-first empty notice. Plus the impure `PracticePlanner` (`@MainActor`) that projects models into candidates, composes `planQuickSession`/`planGoalSession`, and materialises `[SessionBlock]` into a persisted `Routine` (exercises/loops/songs). Plus two **direct producers** of the same `[SessionBlock]` review-then-Save flow: `SongRoutineBuilder` (per-song, ADR 0111) and **`CollectionSessionBuilder`** (whole-collection, ADR 0118) — fans out across every song carrying a collection label (ADR 0033), pools their linked exercises/loops/play-throughs, **dedupes shared units by `uid`**, **sizes** to a `SessionLength` budget (≤ 60-min ceiling) and **arranges** by a player-chosen **`OrderMode`** dial (`.structured`/`.mixed`/`.shuffled`, seeded via a small SplitMix64 `SeededGenerator` so the shuffled modes stay pure/testable), trailing play-throughs capped per length; entered from the Library's **filtered-collection banner** (`CollectionSessionSheet`, a Length + Order configurator shown only when filtered to one collection with ≥1 linked exercise/loop). The planner UI lives in `Features/Practice/` (`PlannerView`, `GoalEditorView`). **A mastery rating carries its conditions (ADR 0169):** `Exercise.masteryTempo` + `masteryNotesPerBeat` and `Loop.masteryAtSpeed` are stamped by the models' own `rateMastery` (the single write path, on the model for the reason `promoteCommand` is), the pure `MasteryReading` decides when a reading has gone **stale** — the command, or an exercise's rhythm, has left the stamp; an *unstamped* pre-0169 rating never is — and `DueScore.masteryTerm` floors a stale rating at `masteryTerm(4)` rather than letting a 5 zero the product. Fixes a live bug: `commitDone` writes the rating and an accepted raise in one commit, so rating 5 and promoting sank the drill to the bottom of the ranking (`ranked` filters on `priority`, not score, so a 0 sorts last rather than being excluded) at a tempo it had never been rated at. The rating is never rewritten (ADR 0070) — only the note that its conditions moved is new |
| `Pocket/Features/Toolkit/` | **Toolkit** — the chords/theory/resources reference hub (ADR 0096, fourth home card, indigo/violet `PocketColor.toolkit`). Relies on an ambient `NavigationStack` (pushed from Home). `ToolkitView` is a landing list of sections; **Slice 1** carries two: **`MyChordsView`** — the `SavedChord` library promoted to a full grid (newest-first), each cell → `MyChordDetailView` (large `ChordDiagramView` + a **Hear** button sounding the voicing as a block chord through the shared `ToneEngine` (ADR 0097 Slice 1) + **Rename** (keeps `SavedChord.name` and the encoded voicing name in step) + **Delete**), **+** opens the existing `CustomChordSheet` in "Save" mode (`confirmTitle: "Save"` — building here *keeps* rather than *inserts*; the chord picker's My chords group reuses the same library inline (ADR 0103), and this screen is where it's managed) — and **`GlossaryView`** — a searchable, area-grouped static terms sheet over `GlossaryTerm.all` (pure `matches(_:)`/`matching(_:)` search). *Hear* on chords lands in Slice 1 (ADR 0097); identifier / scales / ear-training are later slices. **Tuner** (`TunerView` + `TunerGaugeView`, ADR 0115) — the hub's first live-mic tenant: `TunerEngine` (`Core/Audio`, the app's first `installTap`) feeds the pure autocorrelation `PitchDetector` → `TunerReading` (nearest note + cents, spelled by the accidental preference — a detected pitch has no key, ADR 0123) over a curated `Instrument`/`Tuning` catalog (guitar + bass), lightly smoothed (`TunerSmoother`); the screen shows the note + arc-needle cents gauge + nearest string + a `ToneEngine` **Hear** reference, starting on appear (after mic permission) and stopping on disappear/background. A ⚙︎ opens **`TuneSettingsSheet`** (Pocket-native, indigo — not a system form) carrying every preference via `@AppStorage` on `AppSettings.Key` (`tunerInstrument`/`tunerMode`/`tunerTuning`/`tunerReferenceA`/`tunerChimeEnabled`, each with a `resolved…` fallback): **instrument** (Guitar/Bass), **mode** (`TunerMode` Guided/Chromatic — Guided names the target string + direction and shows the string row, Chromatic names any note), **tuning** (reset to Standard on instrument change so a bass tuning never leaks onto guitar), **reference pitch** (A432–A446 stepper, pushed live to `TunerEngine.referenceA` — no re-tap), and the **success-chime** toggle (silences the sound, not the haptic). Reports pitch, never grades (ADR 0070). **Help & FAQs** (`FAQView` + `Core/Help/FAQEntry`, ADR 0145) — the fourth tenant and the same animal as the Glossary: a pure, `Foundation`-only static catalog (`FAQEntry.all`, area-grouped, the same `matches(_:)`/`matching(_:)` search, which matches **inside answers**) rendered by a thin screen whose rows **expand in place** and are **force-expanded while searching**. Also pushed from Settings ▸ About — one screen, two doors (D1). Free with the rest of the hub (ADR 0144 D2), which is the point: it is readable before, during and after a subscription. Carries the app's **first support address**, both as a Settings `mailto:` (version pre-filled in the subject) and as plain selectable text in an answer, because a `mailto:` does nothing without a Mail account (D7). **No price or trial length may appear in an answer** (D6), and the mastery/command-tempo answer *quotes* `PracticeFieldInfo` rather than restating it (D5) — both pinned by `FAQEntryTests` |
| `Pocket/Features/Journal/` | **Journal** space — the read-only, cross-cutting practice-history destination (ADR 0100, the fourth home card, warm-gold `PocketColor.journal`). `JournalTabView` merges journal **notes** (`JournalEntry`) and audio **takes** (`Recording`) across every owner (loops + exercises + songs) onto one newest-first, day-grouped timeline (pure `JournalTimeline` merge/filter/owner-label → `JournalGrouping.byDay`), with a segmented **All / Notes / Takes** filter, a `.searchable` field (song / loop / exercise / template / date, via `JournalTimeline.searchHaystack` token-AND), a **Newest ↔ Oldest** day-sort toggle, and gold owner-attribution captions. Read-only for the entries themselves — authoring stays in the per-owner `JournalSheet` and the compact `QuickJournalSheet`; takes play in place via the shared `RecordingPlayer`. **ADR 0100 amendment (2026-08-05/06)** widens the space's write reach from ADR 0143's session-rows-only delete to **every row**, plus **renaming a take** (the one row identified by nothing but a timestamp) — both through the shared deferred `RowDeletionCoordinator` and a `RenameTakeAlert` keyed on the stable `uid`. **Delete is a press-and-hold, never a swipe**, here and on `TakesSheet`: a swipe is the cheapest gesture in a list, and a reflection or a recording has no source to rebuild it from the way an exercise does — rename keeps its swipe because it destroys nothing. `TakesSheet` also gained the deferred undo it never had (it used to delete the AAC file the instant you swiped), with the host screens' existing delete closures becoming the deferred action. The `JournalEntryRow`/`KindChip` row is shared with `JournalSheet` (owner caption only in the aggregated feed). **ADR 0142** makes that owner caption a **link** (`JournalOwnerCaption` on both the note and take rows): the pure `JournalOwnerRoute.route(for:)` resolves an item to its unit and `JournalOwnerDestinationView` pushes the unit's ordinary run screen — an exercise via `ExerciseRunScreen`, a loop in the **first mode `LoopModeAccess` allows** (trainer → ear → improvise), so an unmeasured loop opens ear training rather than a ramp with nothing to anchor it; a song-owned take and a loop whose audio no longer resolves keep a plain-text caption, and a locked Pro drill opens the paywall (`AccessPolicy.canRun`, ADR 0112). Routes are `Hashable` on the unit's stable `uid`, never `persistentModelID` (ADR 0090). No model change. **ADR 0143** adds the **session entry** — a note owned by a whole routine sitting rather than a unit: written on `RoutinePlayerView`'s session-complete screen (`RoutinePlayerView+Finished.swift`) through the same `JournalNoteComposer`, tagged 🎬 `.session`; captioned by its *snapshotted* routine name (never a lookup through `routineUID` — the routine may be renamed or gone) and rendered with `SessionUnitChips`, a wrapping pill cloud of the units practised, each resolving through `JournalOwnerRoute.route(for:exercises:loops:)` (a **uid lookup**, not a relationship — hence the two extra library `@Query`s on `JournalTabView`) and honouring the same mode precedence + paywall gate the caption does; a unit deleted since the session renders dimmed and untappable. Session unit titles join `searchHaystack`, so searching a drill surfaces the sessions you played it in. **The session's own caption links too (2026-08-06)**: `JournalOwnerRoute` gains a `.routine` case resolved *loosely* from `routineUID` against a third library `@Query`, landing on `RoutineDetailView` (where the Routines library's row-tap goes) and gated on `AccessPolicy.canEditRoutine`; a routine deleted since the session leaves the caption as plain text while its snapshotted name still reads. **`ownerKind` is what selects that branch, never the presence of `routineUID`** — an exercise note written *during* a routine carries one too, and must still open the drill. The Journal space's first write verb was ADR 0143's session-rows-only delete, because a session entry belongs to no unit and no per-owner `JournalSheet` can reach it; the ADR 0100 amendment above widened it to every row. **ADR 0155** gives the space a **compose** verb — a trailing ＋ presenting `QuickJournalSheet(owner: .standalone)`, for the note that belongs to no unit at all ("strings are dead", "ten minutes tonight, that's all there was"). A fifth `JournalEntryOwnerKind` case backed by one additive optional column (`JournalEntry.isStandalone`, no declaration default) — a **stored flag, not an absence**, because absence already means `.orphan` (ADR 0151), and the two render identically while meaning opposites; read **last** in `ownerKind` so the relationships still win (ADR 0143). The ＋ offers **no owner picker**, ever: filing a note against a unit you are not currently practising is exactly what would make `masteryAtEntry`/`commandBpmAtEntry` lie. The **Metronome** is the second door (§8) — a metronome sitting is the clearest case in the app of a surface with no unit whose snapshot could be honest, so it gets the same `QuickJournalButton` and the same sheet. **ADR 0160 reverses that section's two refusals** and makes it a *sixth* owner kind, `.metronome`: what §8 objected to was a bare BPM — a fragment of a *unit's* context with no unit behind it — and a metronome sitting has a complete context of its own, so the entry snapshots **tempo, meter, subdivision and click withdrawal** and renders them as `96 BPM · 4/4 · ♫ · gentle withdrawal`. Stored as **raw values only** (two `Int`s for the meter, `String`s for the two enums — a custom type as a SwiftData attribute crashes on migration on device only), classified by its **own flag** (`isMetronome`, read before `isStandalone`, which it leaves nil so the two partition — never derived from the columns being present, which would make the kind a function of the payload), and pinned **when the composer opens** rather than at Save, because the automator keeps ramping behind the sheet. The withdrawal recorded is `activeWithdrawal` — the tier in force, not the one configured. Caption reads "Metronome" (a constant) and routes nowhere; the pure `MetronomeJournalContext` owns the round trip and the caption; no backfill. The composer's footer becomes a whole owner-supplied sentence (`destinationLine`, replacing `snapshotBlurb` — the two fragments have no honest form here), and the chip row narrows to 🎯/⚡️/🧗/📝 on this surface, dropping the three tags that assert the note was written *during* something. Per ADR 0126 the toolbar folds Progress + Sort into `ellipsis.circle` so the ＋ takes its proper slot; a standalone note **cannot be edited** after writing (§4) — delete-and-rewrite, with the feed's Undo window. **ADR 0174** gives a take a **screen of its own** (`TakeDetailView`, pushed from both this feed and `TakesSheet`; the row splits into two tap targets — the glyph plays, the title line opens — and the caller's `RecordingPlayer` goes with it so one take still plays at a time across a surface and the screen it pushes, delete still routing through the existing deferred, undoable closure rather than a sixth copy of `deleteTake`). It carries a **scrubber** (`TakeScrubber` over the reused `WaveformGesture`/`ABSpan`/`WaveformAmplitude` layer, envelope extracted on open and never stored; the per-tick position is read **only** by the `PlayheadTakeScrubber` leaf, per ADR 0153), a **destructive trim** (pure `TakeTrim` span rules + `TakeTrimmer`'s temp-encode → verify → `replaceItemAt`, so every failure leaves the take byte-identical; entered from the menu, confirmed with the amount it removes, and refused on a whole-take no-op), and a **note** — `Recording.note`, one additive optional mirroring `title`, a field on the take rather than a `JournalEntry`, so it reads beside the audio and is deliberately absent from this feed and its search rail. **ADR 0175** pins notes to *points* in a take: a new `TakeNote` entity on a new `Recording.moments`, **`.cascade`** — the deliberate inverse of `recordings`' `.nullify`, because ADR 0151 keeps a take alive past its loop (the audio cannot be remade) while a moment is a pointer *into* that audio and means nothing once it is gone. `Recording.note` is kept alongside rather than migrated into an optionally-timed list: *what was this take like?* and *what happened here?* are two questions. Moments draw as gold pins on `TakeScrubber` from a `momentFractions` array computed in the body and passed down, and the add control says **Add note here** with **no live timecode** — a label reading `player.position` from that body would re-execute it at the ticker's rate, which is exactly what ADR 0153 confines to the `PlayheadTakeScrubber` leaf; the position is read once, on the tap, and playback pauses. The editor is presented by a plain `TakeMomentDraft` value, so nothing is inserted until Save and there is no `persistentModelID` to flip under an open sheet (ADR 0090). **Trimming rebases the survivors and deletes the rest** (pure `TakeMoments`, unit-tested): inside the keep-span a moment shifts by `keepStart` and goes on pointing at the same playing, outside it goes with the audio it described — clamping to the edges was rejected as a row pointing at playing it was never about — with the count named in the confirmation beside the seconds, doomed pins drawn faint while the handles move, and the whole rebase run only *after* `TakeTrimmer` returns so a failed trim leaves the notes untouched with the file. Empty text is refused (a row has a real delete; the whole-take note's clearing gesture is its only one), and the row glyph now means `hasWriting` rather than `hasNote`. |
| `Pocket/Features/Settings/` | **Settings hub** (pushed from the Home gear) — since **ADR 0162** a list of nine destinations rather than the thirteen-section flat `Form` it grew into, one section per ADR in the order they landed. `SettingsView` is now the hub only; each row pushes a screen owning one coherent group, and states that group's dominant setting as a trailing value (`SettingsHubRow`) so the common visit ends without opening anything — **except `Song player`, blank on purpose: four unranked toggles, none of them "the" answer**. Shared chrome is one `settingsScreen(title:)` modifier; the ⓘ copy lives in `SettingsInfo.swift`. Destinations: **You** (`YouSettingsView` — artist name, ADR 0113, + `ProfileCurationSection`'s instrument/experience/genres/dream/minutes; name commits on submit and on *leaving this screen*, which is earlier than before) · **Red Moon Pro** (`ProSettingsView` — `TrialCountdownRow`, Manage/Upgrade, Restore; lifted from thirteenth place to the top block as *state*, not a preference) · **Appearance** (`AppearanceSettingsView` — System/Light/Dark override ADR 0063, Animate exercises, and `NoteSpellingSection`'s sharps-vs-flats ADR 0123, grouped here because accidentals are *how a note is written*) · **Sound & feel** (`SoundFeelSettingsView` — Haptics + `MetronomeSoundSection`) · **Practice** (`PracticeSettingsView` — Count-in + length, Keep screen awake, Strumming click, and `TempoWarningSection` ADR 0131, which belongs with Count-in since both are the run saying what happens next) · **Routines** (`RoutineSettingsView` — auto-start, advance automatically, rest length, loop song blocks; ADR 0071) · **Song player** (`SongPlayerSettingsView` — **renamed from "Transport"**, engineer's jargon that escaped into the UI: loop-control side + **Show minimap** default on, planner review P1c + **Show marker labels** default on, P2 + **Zoom follows playhead** default off, ADR 0098; **ADR 0163** gives this one screen a *second door* — **holding *Loop controls*** on the waveform status line (`ModeDescriptionLine`, whose tap keeps its gesture-cheatsheet popover, and which already carries the **Follow** chip writing one of these same four keys; chosen over the transport's Loop button, which was built first and moved — the status line is replaced by the A/B strip and the downbeat bar, so "idle only" needs no guard, whereas on the transport `loopActive` tracks only a *saved* loop and an arming span would have left a hold live mid-cycle) presents the identical view as `SongPlayerSettingsSheet` (a `MeterPickerSheet`-shaped wrapper: `NavigationStack` + Done + `[.medium, .large]`), since all four settings describe the screen you're on when you want to change them. The Settings row **stays** — a hold has no affordance, and as the only route it would re-create exactly the unfindability 0162 fixed. Both doors present `SongPlayerSettingsView` itself rather than a copy, so no label or ⓘ string can drift; the four defaults became named `AppSettings` constants (`transportLoopOnLeftDefault` et al, the `exerciseAnimatesDefault` pattern) because each had been an inline literal at *three* sites SwiftUI never reconciles, and two surfaces showing one switch makes that drift visible. Global, unchanged: per-song `Song.showsGridlines` (ADR 0051) deliberately stays a waveform chip rather than joining them) · **Privacy** (`PrivacySettingsView` wrapping `PrivacySection` — one push deeper, checked against ADR 0147's DUAA objection mechanism in 0162 D7 and unchanged in key, mechanism and per-event re-read) · **Help & About** (`AboutSettingsView` wrapping `AboutSection`, ADR 0145 — Version + **Help & FAQs**, a `NavigationLink` to the Toolkit's `FAQView` since Settings is *pushed*, so one screen serves both doors + **Contact Support**, an in-app form `ContactSupportSheet` ADR 0161 replacing the `mailto:` that silently did nothing without a Mail account: reply address **required**, message, and three diagnostics *printed in the sheet* before sending — app version, iOS version, device model; POSTs JSON to the shared Formspree form via `SupportSending`/`FormspreeSender`, a failed send keeps the sheet open with the text intact, and the address still sits as plain selectable text in an FAQ answer, load-bearing twice over since a *rejected* send points the player at it + Privacy Policy + Terms of Use, footer showing the Red Moon brand mark ADR 0061) · **Developer** (`DeveloperSettingsView`, whole file inside `#if DEBUG` — entitlement override, `DebugAudioSection`, first-run resets). Pure IA: no key, default, model or migration changed (0162 D9). Backgrounds transparent (ADR 0063) |
| `Pocket/Resources/Assets.xcassets/` | Asset catalog (ADR 0061): `AppIcon` (the crescent + stars mark, dark-background colouring, on opaque `#0F0F0F`; stays **PNG** — App Store icons must be opaque 1024², no alpha — and is rasterised from the same vector source by `scripts/derive-brand-svgs.py --app-icon`), plus three **vector (SVG)** brand imagesets — `RedMoonLogo` (full lockup, Settings), `RedMoonMark` (crescent + stars, artist-name prompt), `RedMoonWordmark` (text only, Home nav title) — and one **raster** one, `RedMoonProWordmark` ("Red Moon PRO", the paywall header; PNG because the designer ships it as an export, not a lockup, so it is cropped by alpha rather than by path id — `--pro-wordmark`). Each carries a light and a dark appearance: the mark is two-tone, so it can't be one template image tinted in code. All four are **generated** by `scripts/derive-brand-svgs.py` — don't hand-edit them |
| `Pocket/Features/Repertoire/` | Song cards, song info |
| `Pocket/Core/Audio/` | AVFoundation engine, tempo math (pure logic). **Hear — pitched-tone preview** (`ToneEngine`, ADR 0097): a shared, sequence-capable tone service (its own tiny `AVAudioEngine` + single `AVAudioUnitSampler`, separate from the file-playback pipeline, so ADR 0001 is untouched) — `sound(_:)` plays MIDI notes together (block chord), `sequence(_:)` spaces them in time (scale/arpeggio/interval), reading MIDI the models already expose (`ChordVoicing.midiNotes`, `ScaleRun.sequence`→`CAGEDShape.midi`); v1 renders iOS's zero-asset **built-in sampler tone** (no accessible GM bank — a redistributable CC0 `HearGuitar.sf2` would swap in a nylon program over the same path, deferred D4.3); a re-tap cancels any in-flight preview. **Slice 1 wires block-chord Hear on the My Chords detail**; scale/arpeggio/interval preview are follow-up slices. **Practice-take recording** (ADR 0069, mic-only audio journal): `AudioPlumbing.configureRecordSession` — a `.playAndRecord` config applied only while a take is armed (`.defaultToSpeaker` + `.allowBluetoothA2DP` and deliberately **not** `.allowBluetooth`, so output stays A2DP-clean and input falls to the built-in mic — avoids the Bluetooth HFP-collapse); pure **`RecordingRoute`** classifier (private-listening → clean take vs speaker/unknown → bleed nudge, ADR 0069 §2, unit-tested); **`MicPermission`** (iOS 17+ `AVAudioApplication` flow); **`TakeRecorder`** (mic-only → AAC via `AVAudioRecorder`, not tapping the playback engine — engines unchanged, §3); **`RecordingController`** (pre-start arm orchestration: permission → record session → capture → persist → restore; begins the take *before* playback to avoid a mid-play glitch); **`RecordingPlayer`** (one-at-a-time take relisten via `AVAudioPlayer`); **`RecordingStore`** (app-container `Recordings/` dir + delete/size + pure orphan-sweep retention). New `NSMicrophoneUsageDescription` in Info.plist (mic records the user's own playing only; never the app's loop/song output — ADR 0001/0064). Slices 0–3 done on the **loop trainer**: record is a pre-start arm toggle beside Start (loops also gained the count-in); relisten via a **Takes** sheet reached from the one-row `PracticeReviewBar` (Journal + Takes count pills). Exercise/song surfaces reuse the owner-agnostic `RecordingOwner`. **ADR 0069 amendment** (2026-08-05) takes takes to the two **open-ended** surfaces — **improvise** (a `Loop`) and **freeform blocks** (an `Exercise`), both of which already had a cascade `recordings` relationship, so no model work: improvise keeps arm-then-Start (its play button *is* a Start — `ContinuousLoopControls` gains an optional `recorder` + owner-agnostic `onStopped`, and each of the three hosts owns the controller as it already owns the player), while a **Start-less** freeform block gets a direct `RecordTakeToggle` → `RecordingController.toggleTake(owner:context:)`; when that block ticks it also calls `holdRecordSession()` in `.onAppear` **before** the click starts (released on exit) so the `.playAndRecord` flip still lands with nothing sounding and the toggle never changes category mid-stream — the audible glitch the arm grammar exists to avoid. The `routineContext == nil` gate lifts for **these two block kinds only** (no ramp, no command, no logged tempo ⇒ the take is their only record); ramped blocks stay gated, and there is still no `recording` `PracticeRunKind`. A freeform block never opens `ExerciseRunView`, so it carries its own **Takes** entry in the ⋯ menu. **Extended 2026-08-06:** **ear training records too** (ADR 0104 amendment — all three ear hosts own a `RecordingController`, routine ear blocks included, and `EarLoopRunView.finish()` finalises the take before logging and advancing); and both loop modes can now **reach their own takes** — the resting take count under the record button is a button (`TakeSavedNote.onOpenTakes` → `ContinuousLoopControls.onOpenTakes` → the host's `TakesSheet`, since only the host knows the owner), which **stops the bed first** because `TakesSheet` has its own `RecordingPlayer`. Mode copy is parameterised by one `bedNoun` ("backing track" / "loop") so every line naming the bed is built from it. The **ramped** run screens keep their `!isRunning` review-bar gate. **The device pass then found takes being *destroyed* on stop** (2026-08-05): `RecordingController` was the one audio producer holding no session lease, so the last other producer stopping — or anything asserting `.playback` — pulled the session out from under a live `AVAudioRecorder`, `currentTime` read `0`, and the `minTakeDuration` rule deleted the file as an accidental tap. Fixed in three places: the controller now holds two `AudioSessionClaim`s (one per take, one per held session — invariant on the property); the `.playAndRecord` guard moved out of `StandaloneMetronomeEngine` into `AudioPlumbing.ensurePlaybackSession`/`ensureRecordSession` so `PracticeAudioEngine` (improvise loads *after* arming) and `RecordingPlayer` (auditioning a take broke the next one) stop stomping a record session; and `TakeRecorder.stop()` reads the true length off the written file via `AVAudioFile` when `currentTime` reports nothing — a file holding audio is never deleted for looking empty. Repairs the **shipped** loop/exercise surfaces too, whose `.onChange(of: isRunning)` finalise seams run after their engine released the session. **Imported-song custody** (ADR 0148): **`SongFileStore`** — the app-container `Songs/` dir, shaped exactly like `RecordingStore` (create/adopt/delete/size + a pure orphan sweep, `FileManager` injected), where `adopt` **replaces** any existing copy for a `sourceID` so re-import and relink overwrite rather than accumulate; and **`SongAudioResolver`** — the single resolution order (owned copy → legacy bookmark → nothing), replacing three drifted `loadImportedFile` copies across the waveform screen, the loop run and the play-along, only two of which re-minted a stale bookmark. It also **adopts a pre-0148 song on first successful resolve**, which is the migration: no schema version, runs only when the file is provably reachable, and a failure leaves the working bookmark untouched. `SecurityScopedAccess` is now `nil` for an owned copy — a file inside our own container needs no scope |
| `Pocket/Core/Models/` | Song, Loop, Marker, JournalEntry, Exercise (+ closed `ExerciseTemplate` axis chosen at creation & immutable, deriving the `ExerciseKind` renderer — incl. **`.freeform`** (ADR 0136), a block whose whole content is the player's own written instructions in the pre-existing `Exercise.notes`: no tempo, no ramp, no renderer, goal-invisible (absent from `SkillFamilyMap`) but due-scored like any exercise, and no new stored field so no migration; + `awayFromInstrument`, the player's **declaration** (never an inference) that a freeform block needs no guitar, read through the template-gated `declaresAwayFromInstrument` and the one exercise that survives an off-guitar constrained pool — ADR 0139 O6; + `clickEnabled` / `clickBPM`, a freeform block's **optional plain click** (read via `playsFreeformClick`), deliberately *not* a command tempo — no ramp, no promote, still logs `tempoBPM: nil`; + a sibling **`Instrument`** axis (ADR 0116 — guitar/bass, `String`-backed `instrumentRaw`, declaration-default guitar so existing drills migrate as guitar, chosen at creation & defaulted from `Profile.preferredInstrument`) that governs which `Tuning` the fretboard engine renders against, crossed into the highest-first engine through the single pure `Tuning.engineOpenMidi` reversal adapter (golden-tested guitar-byte-identical); **ADR-0116 Slice 3** makes bass render: the run generators take `expanded(instrument:)` (guitar routes to the untouched byte-identical path, so no recipe migration — instrument is threaded from the `Exercise`, never persisted in the payload), and bass scales/arpeggios generate through a dedicated **`BassNeckLayout`** — a 2-octave 4-string box built on the *shared* `ScaleNeckLayout.ascendingTones` ladder (not a truncated CAGED box, not a parallel geometry table), box-only with a single low-string-rooted flagship (the diagonal/3-NPS/passing-tone layouts stay guitar-only); the create step shows a **Guitar / Bass** control (`ConfigureExerciseForm`, defaulted from `Profile.preferredInstrument`) that seeds the flagship and threads into the editors + `NewExercisePlan` + `StrumPattern` / `FretboardContent` (`FretboardRun` finger-pattern — movable, with ADR-0083 **position-shifting**: a per-pass neck climb (`fretShiftPerPass`/`passCount`), a per-string diagonal (`fretShiftPerString`), a `.retrace`/`.restate` come-back (`returnStyle`), and same-string pass seams articulated as `.slide` (drawn by `SlideCue`); all additive, defaults reproduce today's run | `ScaleRun`/`ArpeggioRun` generated from `GuitarScale`/`ArpeggioQuality` interval formulas via the five shared `CAGEDShape` boxes (positions + octaves) — the `GuitarScale` catalog (ADR 0085) spans pentatonics, the major scale and all seven **modes** (Dorian/Phrygian/Lydian/Mixolydian/Locrian borrow their *parent major* box via `relativeMajorSemitones`, filtered to their degrees), plus the **blues** and two **bebop** scales, whose chromatic passing tone no diatonic box holds and so is threaded one fret above its `passingToneAnchorDegree` (♭5/♯5/♮7); the *symmetric* diminished + whole-tone scales aren't box-generatable (not subsets of a major scale); rather than build a new generator they're **drawn** on the custom-scale canvas (ADR 0107), guided by a `ScaleReference` overlay — with a ADR-0083 **`layout` axis** (`ScaleLayout`, additive/decode-time-defaulted): `.box` (default) plus two neck-spanning rules in the pure `ScaleNeckLayout` namespace — `.extended` (the minor/major-pentatonic diagonal linking three boxes with same-string `.slide` seams + per-note box groups for focus; offered as the **two** canonical `ExtendedPentatonicShape` fingerings — slide on A&G or on D&B, each a clean whole-step) and `.threePerString` (the diatonic three-tones-per-string drill, offered by every 7-tone diatonic scale incl. the modes), gated per scale via `GuitarScale.supportedLayouts`; plus an orthogonal ADR-0108 **sequence axis** (`SequencePattern`, additive/decode-defaulted to `.straight`) reordering the *played* run into **thirds / fourths / groups of 3–4** — a pure permutation applied in `sequenceWithGroups`, so `ascendingNotes` and the box labels/anchors are untouched; the editor labels each box by its **root anchor** ("root on low E · fret 5", `CAGEDShape.rootAnchor`, ADR 0091) with the CAGED letter demoted to a caption, and a new drill opens on the **flagship** root-position 6th-string box (badged "Most common" — `CAGEDShape.flagshipPosition`, the box whose run begins on the tonic on the low E; position 5 for the minor pentatonic, not 1), in scales and arpeggios alike | `ChordProgression` of `ChordVoicing`s rendered as chord diagrams — triads fold in as three-note voicings; a pure **`ChordGrip`** (ADR 0084) is a *movable-shape recipe* — relative fret offsets + a root string (E/A, plus D/G for the triad sets) + a quality — that generates a `ChordVoicing` when slid to a root note (auto-named from its own content), never a stored table (M1); the curated Tier 1–2 grips cover E/A-shape triads + 7ths + sus2/sus4/6ths + **9ths** (dom9/maj9/min9, ADR 0101 — the 9th sits a fret below the root on an inner string, so those grips carry sub-root offsets and the placement **bumps up an octave** rather than falling off the nut at a low root) + **power chords** (E/A-shape root+5th+octave, no 3rd, in Tier 1 — ADR 0106) and reproduce the two library barres byte-for-byte (M5); a separate **`ChordGrip.triads`** set (ADR 0109) adds root-position **maj/min triads** on the three upper string sets (G‑B‑e / D‑G‑B / A‑D‑G — rooting on G/D/A, the upper-string 5th putting them below the root so the same octave-bump keeps low roots playable), surfaced as their own collapsible **Triads** Insert category; browsed as chips in the picker's Insert grid — **all of Tier 1** as *Movable shapes* and Tier 2 as a last, initially-collapsed **Sus, 6ths & 9ths** section (ADR 0122); tapping a chip opens a root menu and inserts the placed voicing, which retired `MovableChordSheet` (its four-step family → quality → root → confirm flow reached the same shapes the chip reaches in two) and left **Build** as a single action opening the custom placer — no slide animation, since a progression never physically slides between chords (M6 resolves to a static shape-family + fret label for the chord case); and the **`CustomChordSheet`** custom placer (M4, slice 3) — the Tier-3 escape hatch for anything the curated grips can't voice (jazz shells, extensions, altered dominants, D-root shapes): a **full-screen, tappable chord box** (an editable twin of `ChordDiagramView`) — tap a fret to place it, tap it again to clear, tap the ✕/○ marker above a string to cycle muted↔open, with a position control to slide the window up the neck; each sounded string shows its **scale degree** from the lowest note (pure `ChordVoicing.degreeLabels`/`degreeName`), a live **`ChordIdentifierPanel`** under the board suggests what the shape is called (ADR 0093 — `ChordNamer` reverse lookup, "Looks like Cmaj7" + alternate/inversion chips, tap-to-fill-name, "No common name" fallback, hidden below 3 distinct notes — except a **power chord** (root+5th) names at 2 (ADR 0106); informational, never a gate or grade), the player names it, and it lands as a plain `ChordVoicing` (same output type, renderer untouched; fingers deferred while the shared diagram doesn't draw them) | custom `FretboardDrill` | `StrumChordSheet` pairing a `StrumPattern` with a `ChordProgression` for the Chords & Strum template) payloads, ADR 0068/0065; plus self-rated **`mastery: Int?`** + **`lastPracticed: Date?`** mirroring `Loop`/`Song` — the planner's *need* + *dueness* signals, self-set never graded, ADR 0072/0070; plus **`presetSlug: String?`** (ADR 0112) — seeded-preset *provenance* (additive optional `String`, non-lossy migration; stamped by `PracticePresets` + one-time back-filled onto older installs), formerly the substrate the pure `AccessPolicy` read to grant a free-taste *run* allowance — **inert since ADR 0144** (the allowlists are empty); never a Pro flag, access stays computed live from `isPro`; plus a user-authored **`linkedSongs: [Song]`** repertoire edge — the songs a drill is *for*, inverse of **`Song.linkedExercises`**, the store's **first many-to-many** (`@Relationship(inverse:)` declared on the `Exercise` side only, `.nullify` both ways so neither delete cascades, additive optional → empty set on migration), narrowing the old ADR-0043/0046 "no relationship to `Song`" absolute to *audio/tempo*-free while the audio firewall stands; substrate for the deferred "build a routine for this song" generator, ADR 0111; **authored from both sides** via a shared `LinkPickerSheet` (searchable multi-select over the `@Query`'d catalog, presentation-only — the owning detail sheet holds the persistence): a **Songs** section on `ExerciseDetailSheet` and an **Exercises for this song** section on `SongDetailsSheet` (the latter now also reachable from the Library row's long-press **Details** action, not only the waveform title-hold), each with swipe-to-unlink, **and at creation** on `ConfigureExerciseForm` (a **Songs** section over the same `LinkPickerSheet`, for every template — the picks are *staged* in local state and carried on `NewExercisePlan.songs`, then attached by each host **after** its `context.insert`, since a relationship assigned to a not-yet-inserted model doesn't stick; the section hides itself when the library holds no songs, and the automator's "Save as exercise" seam attaches them too so a link made there isn't silently dropped); the song section also carries **Build a routine for this song** → the pure `SongRoutineBuilder` (Core/Planner) emits `[SessionBlock]` from the song's linked exercises (`.focus`) + its loops (`.focus`) + a `.play` play-through, reusing `RoutineDetailView(generatedSession:)` → `PracticePlanner.materialise` so nothing persists until Save; disabled unless the song has ≥1 linked exercise or loop), **Routine + RoutineItem** (the multi-unit *session* container — ordered typed blocks referencing an Exercise/Loop/Song or a rest, nullify-on-unit-delete; a **loop** block also carries a `LoopRunMode` (`.trainer`/`.ear`, String-backed like `kindRaw`) so the same loop unit runs either the command-anchored trainer or ears-only ear training (ADR 0104 Slice 2), plus **`lastPracticed: Date?`** (stamped when a session starts, drives the home "recent routines" rail) and **`presetSlug: String?`** (ADR 0112 — curated-routine *provenance*, mirroring `Exercise.presetSlug`: additive optional, non-lossy migration, stamped by `RoutinePresets` + one-time back-filled by name so an existing install's Morning Warm-up isn't Pro-locked; never a Pro flag, and never filtered in a `#Predicate`), with a pure `RoutineBudget` pacing layer + pure `RoutineSessionCursor` stepping; ADR 0066/0071, authored by hand and run by the auto-advancing `RoutineSessionPlayer` in the Practice space — exercise/loop/song blocks all playable), **Goal** (the V2-planner input, ADR 0073 — `title`, `weight`, `skillIDs: [String]` indexing the taxonomy, optional `targetSong` relationship, `isMet`; produces `PlannerGoal` for the pure deriver), **LongTermGoal** (the *standing* tier, ADR 0171 — same shape minus `weight` plus `order`, and **deliberately no date field of any kind**: with no horizon stored there is nothing for the app to be late against, so the no-verdict property of ADR 0070 is structural. A separate `@Model` rather than a tier flag on `Goal`, so a long-term goal cannot leak into the Today's-session list through a forgotten filter. `order` is load-bearing twice — it sets the projected weight via `LongTermRank` *and* `SessionBuilder.roundRobin`'s visit order, so rank stays observable past a short session's item count), **Recording** (a mic-only practice take, ADR 0069 — `uid`/`createdAt`/`duration` + a relative AAC `fileName`; polymorphic owner via inverse `recordings` arrays on `Loop`/`Exercise`/`Song`, `ownerKind` derived not stored; files reaped by `RecordingStore`'s orphan sweep. **ADR 0151** turns those three relationships — and the `journal` arrays on `Loop`/`Exercise` — from `.cascade` to `.nullify`: a take or a note **outlives the unit it was made against**, since a loop is a start/end pair and an exercise a recipe, both remakeable, while a recording of someone playing on a particular evening is not. Attribution survives via `Recording.ownerLabelAtTake` / `JournalEntry.ownerLabelAtEntry`, snapshotted at write time through the *same* `JournalTimeline.ownerLabel(loop:exercise:song:)` that renders the live caption, so an orphan reads exactly as it did and simply stops being tappable (the ADR 0142 honesty rule). Both are additive optionals with no declaration default. This aligns loops and exercises with the rule `PracticeRun.unitUID` and session entries (ADR 0143) already followed, and makes the loop-delete toast truthful — nothing but the loop goes), **SavedChord** (a user-saved custom chord, ADR 0095 — standalone/relationship-free library entry: `uid`/`name`/`createdAt` + the `ChordVoicing` as an encoded `voicingData: Data` blob decoded via `voicing`, never a stored voicing/enum attribute; additive new table; saved from the placer's **Save to My chords**, reused via the chord picker's **My chords** group (ADR 0103, tap-to-insert), and **managed** on the Toolkit hub's `MyChordsView` full screen (ADR 0096) with an in-place `rename(to:)`), **Profile** (the local, account-free artist profile, ADR 0113 Slice 1 — a **singleton** `@Model`: `uid` + optional `artistName` + `createdAt`, nothing else leaves the device; created lazily via `Profile.setArtistName` (a fetch-or-create that only inserts a row once a non-empty name is set and clears back to `nil`/no-name when blank, so an untouched install carries no row and the greeting reads name-free); read in views by `@Query` `.first?.artistName`, which feeds the pure name-aware `HomeFeed.TimeOfDay.greeting(name:)` home greeting and is edited in **Settings → You**; the ceremonial "you've earned a name" invitation (`ArtistNamePromptSheet`, a full-screen cover — dimmed Red Moon crest, staged fade-in, signature-style field, soft haptic on commit — gated once by the `artistNamePromptSeen` flag) is offered from Home only after the player has **completed an exercise or captured a loop** (a Home-appearance check, so a loop's offer surfaces on returning to Home); **Slice 2** adds four **curation** fields (`experienceRaw`/`genresRaw: [String]`/`dreamRaw`/`minutesPerDayRaw`) — additive optionals **backed by primitives** with computed enum accessors over the pure `ProfileCuration` enums (`ArtistExperience`/`MusicGenre`/`MusicalDream`/`PracticeMinutes`), never stored enums (migration rule) — written by `Profile.setCuration`, collected by the four-card skippable first-launch `ArtistIntakeView` (gated once by `artistIntakeSeen`; Home surfaces intake **or** naming prompt, never both) and edited in **Settings → Your sound** (`ProfileCurationSection`); two *default-only* consumers wired today — experience → a new exercise's command tempo, minutes/day → the planner's initial session length — with genres/dream held for the Slice-3 planner emphasis mix); **ADR 0116** adds a **`preferredInstrumentRaw`** field (computed `preferredInstrument` that always resolves, falling back to guitar; written by `Profile.setPreferredInstrument`, committed **separately** from `setCuration` — which overwrites its four fields as a set, so sharing a writer would let an instrument change clear the curation answers) → the instrument axis default a freshly created exercise picks up; reachable since 2026-07-30 from the **Instrument** row in Settings → Your sound (until then the field had readers but *no writer*, so every consumer read the guitar fallback forever — the seam that made ADR 0128's automator instrument fix invisible), **PracticeRun** (the append-only **practice log**, ADR 0117 Slice 1 — one row per *completed unit-run*, not per practice sit: `startedAt` + `durationSeconds` + a raw-`String` `kindRaw` with a computed `kind` accessor (never a stored enum — the documented device-only migration trap), **loose id copies** `unitUID`/`routineUID` rather than relationships so deleting an exercise or routine can never delete the minutes you spent on it, and the factual tempo pair `tempoBPM` (exercises) / `tempoPercent` (loops, percent-of-original per ADR 0082 — never both set) alongside `notesPerBeat`, the rhythm the tempo was measured in (ADR 0121, without which a trajectory reads a rhythm change as progress). Additive new table; every attribute primitive, every reference optional. Written **once per completed run** at each run screen's natural-completion hook — which covers a routine block and auto-advance, where the Done screen would not — via the single `PracticeLogWriter`; a hand-stopped run logs nothing. All four practice surfaces write: exercise + loop ramps on `onRampFinished`/`onFinished`, an **ear-training block** on its **Done** (which *is* its completion — an ear block has no ramp, ADR 0104), and a **song play-along** when the track ends. A *looping* play-along and standalone `EarTrainingSheet` stay unlogged by the same rule, not by omission: they're open-ended, and "however long the screen was open" isn't an honest length. There is deliberately **no `recording` kind** despite the ADR listing one — a take is always captured *during* an exercise or loop run that already logs, so a second row would double-count the same minutes. A song row carries no `unitUID` (`Song` is the one model with no business `uid`); it carries the minutes and the day, which is what every window stat reads. Read by `ExerciseDetailSheet`'s tempo trajectory today; the Progress screen is Slice 2), SongRef (`Marker` now lives in its own `Marker.swift`; `Loop` split into its own `Loop.swift` — ADR 0111, to keep `Song.swift` under the 400-line cap); **ADR 0135** adds an additive **`isBackingTrack: Bool = false`** to `Loop` — the player's claim that a section is a bed to solo over, set in the loop settings sheet's **Backing track** section (whose footer is *guidance*, not a check — the app verifies nothing about bar boundaries) and carried by `LoopEditSnapshot` so Undo covers it; deliberately not `LoopType` (single-select, answers *what material is this*) and not a `tags` string (open free text, string-matched — this drives a filter, a surface, and at Slice 3 planner resolution, so it must be exact). It unlocks `LoopRunMode.improvise` and the 🎸 `EntryKind.improvise` journal tag; **ADR 0119** adds an additive **`isFavorite: Bool = false`** to `Exercise`, `Routine` and `Loop` (a manual pin, never a grade — feeds no algorithm; declaration-default so lightweight migration fills pre-0119 rows with `false`), surfaced by a per-list favourites filter (star toolbar toggle + leading-swipe + row star) in each of the three Practice libraries — no Home rail / cross-type aggregation (that surface was cut before shipping); **ADR 0167** adds **`ReferenceLink`** — *where you learned it*: a `uid` + `title` + an optional free-text **`note`** (ADR 0167 revision — *what you took from it*, as against the title's *what the resource is*; a plain non-optional `String` with a declaration default, rendered as a two-line-capped third row line under the site) + `urlString` + `order` + `dateAdded` + a `String`-backed `kindRaw` (`.link` today, `.image` carried from day one so the sequenced-second image half is a pure addition rather than a retype after the freeze), owned polymorphically by **four typed optional relationships** (`exercise`/`song`/`loop`/`routine`, exactly one non-nil — the ADR 0066 R4 shape, not a generic `ownerKind + ownerID`, so SwiftData keeps referential integrity). ⚠ The owner inverses **`.cascade`**, the deliberate opposite of the `journal`/`recordings` `.nullify` ADR 0151 chose: a note or a take is a record of *you* and outlives its unit, while a reference link is a record of *the owner* and means nothing once the owner is gone. Never filtered in a `#Predicate` (optional-relationship freeze). All writes funnel through **`ReferenceLinkStore`** (`Core/Models/ReferenceLink+Owners.swift`, over a `ReferenceLinkOwner` protocol the four models conform to) so the single save-time gate — the pure `ReferenceURL`, an **`http`/`https` allowlist** that reads a scheme-less paste as `https` and refuses `javascript:`/`mailto:`/`file:`/custom schemes — cannot be skipped by a view, and so the contiguous-`order` renumbering can't drift between surfaces. Surfaced by a shared `ReferencesSection` (`Pocket/UI/`) on `ExerciseDetailSheet`, `SongDetailsSheet`, `LoopEditSheet` and `RoutineDetailView`, plus a read-only `ReferencesCard` on `RoutineBlockPreview`. The routine surface is the odd one out on three counts, all downstream of it being a **sandboxed** editor: gated on `existsInStore` (so a provisional generated session isn't silently persisted through the relationship), writing into that screen's child `editContext` rather than the environment's (a link inserted through the app context while pointed at a sandboxed routine is a **cross-context relationship**, not a preference), and **read-only outside edit mode with no per-change save** — outside edit mode there is no Save to commit it, and inside it a per-change save would commit whatever block rearrangement was pending. Links there follow the screen's Cancel-discards/Save-keeps contract; the other three edit live models and write immediately, as linked songs already do beside them. **Deliberately on no run screen** (ADR 0077 strips authoring/review from the run; a link tapped mid-session leaves the app — `LoopModeIdentityHeader` is the recorded insertion point if that's ever reversed). **No network of any kind**: no title fetch, no thumbnails, no favicons — the row's subtitle is the host derived locally, and the app's one outbound request (Formspree, ADR 0161) stays one. The paste affordance is a SwiftUI **`PasteButton`**, never a `Button` that reads `UIPasteboard`: the first build read the clipboard *in `body`* so it could name the site it would paste, which raises the system **Allow Paste?** prompt merely on opening the sheet and re-reads on every render — it hung the app for 60s under UI test. The tap is the consent; nothing reads the clipboard until the player asks |
| `Pocket/Features/PracticeLog/` | **The Practice log screen** (ADR 0117, Slice 2; renamed and un-buried by **ADR 0176**) — the payoff over the practice log, reached from a **row on the Journal, above the timeline**, rather than Home or the ⋯ menu. Called *Practice log* and not *Progress* because "progress" names a direction and every measure here is effort (ADR 0070); the name now also agrees with `PracticeLog` / `PracticeRun` underneath. (The ADR said "make the existing `PracticeStatsCard` tappable"; that card was in fact dropped from Home in the 2026-07-09 hub rework and has been dead code since, so the entry point moved to the space that already *is* read-only practice history — leaving Home's ADR 0102 grouping untouched until the deferred year tier and card evolution land together.) `PracticeLogView` runs the queries, maps to values and renders three sections: **This week** (`WeekMinutesChart` — seven daily bars, empty days *drawn* as a stub because a gap is information, exactly one direct label on the busiest bar, weekday initials from the locale's own symbols, and no y-axis/target line/average/last-week ghost, each of which would turn a quiet week into a verdict), **This month** (`MonthHeatmap` — a contribution calendar on a **single-hue, four-step** ramp scaled to the month's *own* busiest day, with unpractised days a distinct neutral rather than step zero so "nothing" never reads as "a little", plus a Less→More key, the longest day, and new tempos) and **All-time** (hours truncated never rounded up, sessions derived by grouping runs on time, the start date, the hour-milestone wall where unreached marks stay visible so the wall doesn't rearrange, and the inventory counts demoted to supporting texture — they measure library size and can go *down*). A fresh install gets **one calm statement**, not three empty sections: a screen you navigated to can't hide the way the old derived card did |
| `Pocket/Core/Theory/` | Objective harmonic analysis, SwiftUI-free (ADR 0093). **`ChordNamer`** — reverse-lookup chord naming: a pitch-class set (+ optional bass) → ranked `[ChordCandidate]` matched against the `ChordQuality.catalog` common-practice table (triads, 6/6-9, the seven common 7ths, add9); root-position preferred over inversions (slash names like `C/E`), symmetric chords (dim7/aug) return all true roots, sharp-spelled with no key assumed (agrees with the board). Keyless (ADR 0086) and deterministic; a thin `candidates(for: ChordVoicing)` adapter is its first consumer (the chord identifier, slice 2). The shared theory core the ear-training space (ADR 0094) will also consume. **`ScaleReference`** (ADR 0107) — a scale as a name + interval formula → pitch classes, reused across `GuitarScale` **plus** the symmetric scales (whole-tone + both diminished modes) it can't box-generate; drives the custom-scale canvas's tracing guide, never the generator |
| `Pocket/Features/Paywall/` | **The Red Moon Pro paywall** (ADR 0112, rescoped by **ADR 0144**). `PaywallView` — one on-brand, theme-aware screen (crescent seal · Futura · design tokens): contextual `PaywallTrigger` headline, three value lines, **Annual pre-selected** (£49.99/yr · "≈ £4.17/mo · best value") over Monthly (£5.99/mo), a trial-aware CTA, an opt-in **"Remind me before the trial ends"** toggle (permission asked *before* the purchase, ADR 0144 D6), **Restore Purchases**, and the App-Review-required auto-renew disclosure + Terms/Privacy links. **Trial length is never hardcoded** (ADR 0144 D5): the CTA and disclosure format `TrialPeriodCopy` from the *selected* product's `introductoryOffer.period`, and eligibility is read per plan rather than always the annual one. Split across `PaywallView.swift` (chrome) and `PaywallView+Products.swift` (products, prices, trial copy, buy/restore) for the 400-line cap. `PaywallHost` (applied once at the app root, **inside** `.environment(store)`) bridges the store into the **preview-safe** `\.isPro` + `\.presentPaywall` environment (defaults: free / no-op) and hosts both the shared sheet **and ADR 0144's once-per-launch `fullScreenCover`** — raised only once `hasResolvedEntitlements` is true, so it never flashes at a paying subscriber, dismissible via "Not now", and never seen under `-uiTesting` (which forces the DEBUG Pro override). `TrialCountdownRow` ("Trial ends in N days · Manage" → `.manageSubscriptionsSheet`) renders on Home and in Settings while a trial runs, and nothing otherwise. **Gate seams: the Home destinations that lead into the workbench** — Practice, Song library, Today's session — plus "Jump back in" and the recent-routines rail, all visible-and-locked with a lock glyph; **the Toolkit and the Journal are deliberately ungated** (ADR 0144 D2 — your own notes/takes/progress are never held hostage by a lapse; the Journal's caption links back into a unit stay gated). The ~20 inner `AccessPolicy` gates remain as defence in depth (they re-lock a screen already open when a trial lapses). A DEBUG Settings control forces the entitlement + previews the paywall |
| `Pocket/Core/Monetization/` | **Red Moon Pro entitlement** (ADR 0112, superseded on the tier axis by **ADR 0144 — one app, one price**). The free/Pro content split is **retired**: `ExerciseTemplate.authoringTier` is `.pro` for every case and `canAuthor` / `canRun` / `canAuthorRoutine` / `canEditRoutine` / `canAddRoutineUnits` / `canRunRoutine` all answer plain `isPro`. `AccessPolicy` is deliberately **kept**, along with every call site and the `isFreeTastePreset:` / `isFreeTasteRoutine:` parameters: `freeTasteSlugs` and `freeTasteRoutineSlugs` are now **empty sets**, and are the seam a future free line would return through as a one-file change (ADR 0144 D3) — pinned by `AccessPolicyTests`, which also proves the seam still opens when a provenance flag is passed. Foundation-only and exhaustively unit-tested; `isPro` passed in. The free surface is the **Toolkit** (zero `isPro` reads) **and the Journal** (read-only history; its caption links out to an exercise/routine still gate). Seeded first-run content (6 exercises · 1 routine · 1 song) is unchanged and is now **trial content**, not a free taste; `presetSlug` survives as seeding provenance only. **`StoreManager`** (`@MainActor @Observable`, StoreKit 2) is the single entitlement source of truth — `isPro` from `Transaction.currentEntitlements` + a `Transaction.updates` listener, `loadProducts`/`purchase`/`restore`, plus `hasResolvedEntitlements`. `refreshEntitlements()` also runs on **every scenePhase → `.active`** (wired at the app root): an expiry with no renewal creates no transaction, so `Transaction.updates` never fires, and without the foreground re-check a lapse held every gate open until the next cold launch — device-verified in sandbox 2026-08-07, where the trial countdown (which reads a stored date, not StoreKit) vanished on time while Pro stayed unlocked and the renewal state (`currentExpiration` / `willAutoRenew`) read from `Product.SubscriptionInfo.status`; the pure `resolveIsPro(entitled:debugOverride:betaGrant:)` keeps the decision testable and honours a DEBUG override. **Temporary (closed beta, 2026-08):** a build running in the StoreKit **sandbox** environment — which for a distributed build means TestFlight — is entitled outright via `betaGrant`, because TestFlight ships Release builds where `debugProOverride` doesn't exist and every invited tester would otherwise meet the launch wall on each cold launch. An App Store download reports `.production` and is unaffected, pinned by `StoreManagerTests`. The `AppTransaction` read is skipped in DEBUG: with no cached app transaction it falls back to a refresh, which raises a system App Store sign-in prompt that lands on top of a running UI test and makes the app untappable. **Remove before the next submission** — `TODO(beta)` is the grep target; see `docs/plans/beta-testing-plan.md`. **`TrialReminder`** (ADR 0144 D6) owns one **local** notification (fixed identifier, so rescheduling replaces rather than stacks — **no `aps-environment`, no push**) and the app's own persisted trial-end date, recorded at the one moment it's knowable: a purchase made while intro-offer eligible. Its decisions are the pure, unit-tested **`TrialReminderPlan`** (says nothing when auto-renew is already off, when the trial has passed, or when the 24h lead time is behind us) and **`TrialPeriodCopy`** (the paywall's trial wording, derived from the product). `StoreManager.onSubscriptionStateChange` is the composition seam, wired at the app root, so StoreKit gains no knowledge of UserNotifications. Local `Configuration/RedMoonPro.storekit` (Annual £49.99 / Monthly £5.99 + **1-month** intro, group "Red Moon Pro") drives local/sim testing only — **the live trial length lives in ASC**. Gate at **read** time — nothing about Pro persisted on a model |
| `Pocket/Core/Stats/` | **The practice log's read side** (ADR 0117, Slice 1) — `SessionRecord` (the plain value every stat is computed over, so no aggregation ever touches a `@Model`) + `PracticeRunKind` (exercise/loop/earLoop/song/recording, plus an `other` fallback for a row written by a newer build); **`PracticeLog`** — the pure aggregation: `weekInterval`/`monthInterval`/`dayInterval` honouring the calendar's own `firstWeekday`, `dailyBuckets` (which emit **empty days too**, so a week chart keeps its seven-bar shape), `daysActive` as a **bare count** (the "4 of 7" denominator is held with the deferred streaks — a denominator states a target), `bestDay` (earliest day wins a tie, so a re-render can't reshuffle it), `sittings` (a sit is derived by **grouping runs on time** — a 30-minute gap measured from the *end* of the previous run, so six routine blocks are one sit and morning/evening are two) and `lifetime` (hours **truncated**, never rounded up; `since == nil` on an empty log rather than an invented start date), plus **`lastPracticedByUnit`** — the latest `startedAt` per `unitUID`, the log's one **non-stats** consumer and the planner's whole time axis (ADR 0137): `Loop` carries no stored `lastPracticed`, so `PracticePlanner.library` fills `PlannerLoop.lastPracticed` from this map instead of the `nil` it used to hard-code, which had pinned `DueScore`'s time term at maximum for every loop forever. Rows with no `unitUID` are skipped (a play-along logs none), the run **kind is deliberately not distinguished** (an `.earLoop` row is that loop's recency for trainer ranking too — the question is *when did you last work on this material*), and only *completed* runs are in the log at all, so it measures work done rather than screens opened. Retroactive by construction — no field, no migration. Exercises and songs keep their **stored** value: theirs means "a run started", this means "a run completed", and switching them is a live ranking change owed its own decision. Two invariants everything obeys: a run is attributed wholly to the day it **started** on (never split across midnight), and minutes round **once** from summed seconds. **`TempoTrajectory`** — one drill's own tempo history, grouped by `notesPerBeat` so only the **most recently practised rhythm** is plotted and the runs at other rhythms are *counted and admitted* rather than silently dropped (ADR 0121: 90 in 16ths against 90 in quarters is not "no progress"; an unstated rhythm is its own group, never defaulted to quarters); a single run returns `nil` (one point implies a direction it hasn't earned) and a downward line is reported as plainly as an upward one (ADR 0070). **`PracticeLogWriter`** — the one insert path (mirroring `JournalWriter`), called from the run screens' natural-completion hooks so a standalone run and a routine block share one seam; a hand-stopped run logs nothing. **Slice 2** adds **`TempoRecord`** — the runs that went faster than that drill had ever been logged at, ceilinged **per rhythm** (a move to sixteenths starts a fresh ceiling rather than reading as a collapse, and a drop to quarters can't fake one) and always measured against **all** history rather than the window being displayed, so last January's 120 isn't re-announced every month; the first run at a rhythm is never one, since there was nothing to beat — and **`PracticeProgress`**, the whole Progress screen computed in one pure pass (`week`/`month`/`allTime` + the 10/50/100/500 hour wall), so the view does no arithmetic at all. `now` and `calendar` are parameters, never `.now`/`.current`, so every boundary is testable. **No year tier and no placeholder for one** — an empty struct would invite filling it in early |
| `Pocket/Core/Services/` | **Empty** (`.gitkeep` only). Was reserved for MusicKit / persistence / sync / AI client; none of them landed here — persistence is SwiftData in `Core/Models/`, analytics in `Core/Analytics/`, and MusicKit, CloudKit sync and the AI client are unbuilt. Kept as a placeholder; delete it or fill it, but don't read this row as an inventory |
| `Pocket/Core/Testing/` | **The app↔XCUITest contract** (ADR 0146, pass 2). `UITestHooks` — the launch argument and the readiness identifiers, **compiled into both the app and `PocketUITests`** (`project.yml`), because a UI test runs in its own process and cannot `@testable import` the app; the alternative is the same literal written twice, and a rename that stops matching doesn't fail fast — it waits out the full timeout and then blames the wrong assertion. Strings only, deliberately: an app type here would mean something different on each side of the boundary. `UITestRuntime.isActive` is the single answer to "is XCUITest driving this process?", replacing five hand-written `CommandLine.arguments.contains` copies (`Analytics`, `StoreManager`, `OrientationGate`, `HomeView+ProfileMoment`, `RowDeletionCoordinator`) — one place to read to learn what the app does differently under test. The one identifier today is `home.seedingComplete`, raised by `HomeView+Seeding.seedingMarker` when first-run seeding finishes; Home paints before that `.task` completes, and the gap between those two moments is what every UI test used to guess at |
| `Pocket/Core/Analytics/` | **Anonymous product analytics** (ADR 0120, **region-split by ADR 0147**) — one of the app's **two** off-device paths, and the only one that can send without the player doing anything in the moment (the other is the ADR 0161 contact form, which sends only on an explicit tap). `AnalyticsPolicy.consentModel(regionCode:)` is the one pure rule deciding which model applies: **`.ask`** (EEA + CH — ePrivacy Art 5(3), default **off**, ADR 0120 unchanged) or **`.notify`** (UK + rest of world — DUAA 2025 Sch A1 para 5, default **on**, disclosed by a footnote in `ArtistIntakeView` that sits above the bottom bar so **Skip cannot escape it**). Region decides only the *default*, never whether an event may send. **`GB` is deliberately not special-cased** (the UK is not in the EEA) and `nil` → `.ask`. The default is written once at launch by `AppSettings.seedAnalyticsDefaultIfNeeded` rather than expressed as an `@AppStorage` literal: there were **three** separate `false` defaults that had to agree, and flipping one alone would show an **off** toggle to a player who is **on**. Seeding also makes an explicit ADR 0120 decline survive forever, pinned in both directions by `AnalyticsPolicyTests`. `AnalyticsConsentSheet` takes a `mode` — `.ask` is the 0120 screen verbatim, `.notify` is a **transitional catch-up for pre-existing installs only** (delete it once none remain). `AnalyticsEvent` is the complete **closed vocabulary** (13 events): an enum whose associated values are only other enums, `Int`s and `Bool`s, so there is no `String` parameter anywhere in the type and user-authored text (song titles, file names, journal notes, the artist name) is impossible to emit *by construction* rather than by review — pinned by `AnalyticsEventTests` and by the repo's only SwiftLint `custom_rule` (`analytics_event_no_free_strings`). Event names are a **frozen wire format**: renaming a Swift case is free, changing an emitted string breaks the dashboard series permanently. Nothing that grades playing (ADR 0070 intact — no mastery, tempo or accuracy). `Analytics` is the `@MainActor` dispatcher every call site talks to (mirroring the `haptic(_:)` seam — no environment plumbing), holding one `AnalyticsSink`; the consent gate is a **single early return** re-read on every send, so Settings ▸ Privacy takes effect on the next event with no relaunch, alongside `-uiTesting` and Xcode-preview suppression. The pure `AnalyticsPolicy.shouldEmit` is the testable rule. `NoOpSink` is the default, `RecordingSink` backs tests, and `AptabaseSink` is the vendor — main-actor confined because the SDK is not concurrency-safe (a non-final `NSObject` subclass compiled in Swift 5 mode), started **lazily on its first delivered event** so it can only ever begin after consent, and inert on a missing/empty/unresolved app key (which is what ships today — no Aptabase app exists yet). Consent itself is asked once, **after a first practice** rather than in the first-run intake, as the last rung of the `HomeView+ProfileMoment` ladder |
| `Pocket/Core/Support/` | **The in-app contact form's engine** (ADR 0161) — the app's second and last off-device path, and its **first `URLSession`**. `SupportRequest` is a pure `Foundation`-only value type (message · reply address · `SupportDiagnostics`) owning validation and the Formspree payload, so both are unit-tested away from the form and the network. **`looksLikeEmailAddress` is a typo catcher, not a validator, and must not grow into one** — Formspree does the authoritative check, and a player locked out of the support form by our own regex has no way left to tell us. `SupportDiagnostics` carries exactly three facts (app version + build, iOS version, `uname` model identifier) and its `summary` is read by **both** the sheet and the payload, so what is shown and what is sent cannot drift — `testPayloadCarriesNothingTheSheetDoesNotShow` fails if a fourth field appears. `SupportSending` is the one-method seam that makes the Formspree choice reversible (a second conformance + a different URL, no view or test moves); `FormspreeSender` POSTs JSON to the shared form — **`Accept: application/json` is load-bearing**, without it Formspree redirects to an HTML page and every refusal reads as a 200. `SupportSendError` splits `.network` (retry) from `.rejected` (don't — go to the plain-text address). `RecordingSupportSender` backs tests and previews; no test touches the live endpoint |
| `Pocket/UI/` | Shared components, design tokens (`PocketColor` — light + dark appearance, ADR 0062; appearance override + vibrancy retune, ADR 0063; blue **`library`** identity + `libraryCardWash`/`libraryCircleWash` for the home Song-library strip, baked per-appearance not opacity-blended). **`AdaptiveLayout`** — the iPad adaptivity groundwork (ADR 0105): `PocketLayout` (pure `readableContentWidth` cap + `contentWidth(available:cap:)`) + a `readableWidth()` modifier that caps content to a centred readable column at *regular* width and is a no-op at compact width. **Dormant on the iPhone-only v1 build** (`TARGETED_DEVICE_FAMILY: "1"`) — takes effect only on iPad (after the eventual universal flip) / iPhone Pro Max landscape; verified via regular-width previews. Applied to every top-level single-column screen — Home, Library, Practice hub, Toolkit, Journal, Settings (the full-bleed waveform/metronome/run screens are deliberately left uncapped). Sheet iPad-sizing and a root master-detail split are **deferred to the universal-flip PR** (only observable on a real iPad, so unverifiable while the flag is `1` — see ADR 0105 §5). **`NavigationBarStyle`** (ADR 0110) — one global `UINavigationBarAppearance` applied once from the app delegate that renders every `.navigationTitle` in Futura-Bold (`UIFontMetrics`-scaled, `Ink`), since SwiftUI has no native title-font hook; title font only, backgrounds untouched, the two `.principal` screens unaffected. **Picker surfaces** — one shared `PickerItem` (title + optional context line, identified by its own value) feeding two components, so a choice reads the same wherever it's made: **`OptionListSection`** for a *bounded* option set as a `Form` section with a checkmark and a footer (the metronome sheet's three groups, `MeterPickerSheet` on the run screen) and **`SearchablePickerList`** for an *unbounded* library list, pushed and `.searchable` with an optional clearing row (a goal's target song, v2 close-out N3). Matching is the pure, unit-tested `PickerSearch` — case- and diacritic-insensitive, and it searches the **context** line too, since which half of a song a player remembers is a coin toss. Both replace popup `Menu`s that had outgrown themselves: a menu scrolls, clips its first row, truncates long labels and has nowhere to put prose |

## Environments

| Env | App build | Backend |
|---|---|---|
| Local dev | Debug scheme, simulator/device | Local proxy (or none, for Phases 1–3) |
| TestFlight | Release scheme, merge to `main` | AWS dev/prod stage |
| App Store | Tagged release | AWS prod |

## Status

**Release state (2026-08-08).** **1.0** was approved 2026-07-28 and went live
2026-08-07 (build `1.0.0(2)`); it took **zero downloads** and was removed from sale
the same day, deliberately — it predates the paywall, ADR 0148's imported-song
custody and the Apple Music permission fix. **1.1 (3)** was submitted 2026-08-07
and is *Waiting for Review*, together with the two Red Moon Pro subscriptions and
their subscription group. ⚠️ **Availability must be re-enabled when 1.1 is
approved**, or it ships approved and undownloadable with no warning in ASC.

The phase numbering below is historical. Phases 1 and 2 are **built and shipped**;
the V2 planner, Journal, Toolkit, recording, tuner and the Pro paywall have all
landed since. Treat "Phase 3 / next" language in the closing paragraph of this
section as superseded — `CHANGELOG.md` and `docs/backlog.md` are the current view.

### The waveform practice screen

The **waveform practice screen**: a fixed practice
cockpit over a scrollable reference area, with named/editable loops & markers
(ADR 0003). Real playback runs through `PracticeAudioEngine` — play/pause/seek,
pitch-preserving speed, and **seamless, click-free region looping** (a crossfaded
`.loops` buffer, ADRs 0006 & 0008) — fed by an imported file's real audio. (The
generated arpeggio `SampleToneGenerator` survives only as a preview/dev sample;
the bundled demo song was dropped with ADR 0148 §7.) Playback surfaces on the **lock screen /
Control Center** (title, artist, play/pause only — plus a default **Red Moon
crescent artwork**, since imported songs carry no cover art) via a
`NowPlayingController` bridge over `MPNowPlayingInfoCenter` +
`MPRemoteCommandCenter`, driven by a pure
unit-tested `NowPlayingState`; leaving the screen **stops** audio and removes the
global command targets (`onDisappear` → `endPlaybackSession`), while locking the
phone mid-practice keeps it playing (ADR 0025). Interaction: **tap = seek, drag = scrub, hold-drag = select a loop, pinch = zoom**
(a **page-mode** viewport — the window holds still and the playhead sweeps/pages across it,
with a Fit / 1× reset; ADR 0010 — and a deep zoom **re-downsamples the visible window from
the source file** for crisp detail, debounced + cached, ADR 0020);
loops are created through the **A/B span** — the single creation primitive (ADR 0041):
the transport is flanked by two big **circular identity buttons** — **Marker** on the far left,
**Loop (A/B)** on the far right (V1 feedback #1; **Click** moved to the speed bar, ADR 0027 / 0030 /
0041). The Loop button lights while a span forms and cross-fades to the active-loop colour strip. Tap **A/B** to drop A at the playhead, play along, tap
again to close an ephemeral **A↔B span** that loops with no ✓/✗ gate; drag its labelled
**A / B handles** to refine it in place, **Save as loop** to persist it, **✕** to clear.
Dragging a saved loop's **edge knob** lifts it back into A/B for a range edit (**Save
changes** writes back). **Hold-drag** the waveform is the spatial set (A pins at the
playhead, the drag sets B; ADR 0005 round 5). The transport
bar carries a **rewind · pause · forward** playback cluster whose outer glyphs depend on whether a
loop is armed (ADR 0124): **armed** → restart / prev-loop / next-loop, as ADR 0030 shipped them;
**idle** → **−N / +N second skips** (`gobackward.10`/`goforward.10`, clamped to the song by pure
`TransportSkip`), holding either for the 5 · 10 · 15 · 30 · 1 min increment menu shared by both and
stored in `AppSettings.Key.transportSkipSeconds`. Idle rewind-to-restart and the never-implemented
cross-song forward are withdrawn with it. Plus an **active-loop colour strip** with an ✕ deactivator (ADR 0030);
a left-edge **swipe-back guard** stops a scrub from popping back to the library mid-adjust.
On **release**, a dragged A/B edge / tap-seek **snaps to a nearby marker or
saved-loop edge** within an on-screen tolerance (pure `WaveformGesture.snap`, light haptic;
the continuous scrub stays free; ADR 0021). When **range-editing a saved loop**, that edge
snap is **neighbour-aware** (ADR 0099): a facing neighbour loop's edge yields — its catch
radius shrinks to half the gap (`WaveformGesture.yieldedTolerance`) so a deliberate small gap
between two loops survives — and **holding the edge still mid-drag** (~400 ms) suspends snap
for that drag (medium haptic) for arbitrary free placement. The **minimap** snaps a released seek to a
nearby **marker or saved-loop edge** (but not beats — the full-song strip is too compressed
for the grid to land cleanly), so a tap or drag near a marker dot or loop boundary catches it. When a song has a **BPM and a downbeat anchor**, a
a **beat grid** is drawn behind the bars (bar-start downbeats brighter, density-aware on
zoom) and its beats join the snap candidates, so edges and **tap**-seeks catch the pulse too — pure,
unit-tested `BeatGrid`, grouped by the song's **time signature** (`beatsPerBar`, ADR 0051;
default 4/4). **ADR 0154** lets a song carry **more than one anchor** — additive
declaration-default `Song.extraDownbeatSeconds: [TimeInterval]` beside the primary
`downbeatSeconds`, read together as `downbeatAnchors` — because one tempo plus one anchor is a
straight line and phase error accumulates as ∫(played − stored)·dt away from it, so a click set
against a hand-played, unquantised beat locks, slides out and slides back. Beats run forward from
each anchor to the next, **restarting the bar count at each one** (an anchor *is* a 1), under one
invariant: no two beats may be *closer* than half an interval — which drops a beat crowding the
next anchor and collapses near-duplicate anchors. In the downbeat bar, ✓ **corrects from the drop**
and leaves the original 1 alone, **Move the 1** replaces the original and leaves corrections alone,
and a count + undoable **Clear** removes them. The single-anchor `BeatGrid` entry point delegates
to the plural one byte-identically. A **scrub** seek deliberately drops the beat grid and catches only the sparse
landmarks (markers + loop edges), the same set the minimap uses, so a free scrub between beats
lands where the finger lifts instead of magnetizing to the pulse (ADR 0080). A per-song **Grid** toggle on the "Loop controls" row shows/hides it, appearing
only once a grid exists; when a **tempo is set but the 1 isn't** (`needsDownbeat` — a BPM-only commit,
since ADR 0022 won't guess the phase) that slot shows **Set the 1** instead, entering downbeat
placement, so the half-set state explains itself rather than reading as a broken grid. The same row
carries a **Follow** toggle — the ADR-0098 "zoom follows
playhead" preference surfaced where the gesture is (it reads/writes the Settings key, so the two stay
one switch), since anchoring the pinch to the playhead rather than to your fingers is a per-moment
choice. **Holding the metronome** on the speed bar opens a tempo
editor (`BPMSheet`) — ADR 0124 retired the "Set BPM" capsule, giving its slot to **repeat**; while
the song's tempo is **unknown** the metronome also badges itself (`plus`) and takes a plain tap, so
the state where nothing grids, snaps or clicks doesn't depend on discovering a hold. The editor is: **tap-tempo** (each tap captures song-time, so in-loop / slowed tapping
still reads the true tempo — pure `TempoMath.bpm(fromTapTimes:)`) or **manual** entry, plus
**the 1** placed by dragging a waveform handle that **snaps to the loudest transient**
(pure `TempoPeaks`) or marked at the playhead. Tempo is stored full-precision in
`Song.preciseBPM` (`Song.bpm: Int?` is the rounded display mirror; `tempoBPM` feeds the grid)
so it doesn't drift across a long song; long-press the BPM readout to re-open the editor.
The editor can also **estimate the tempo and the 1 from the audio** — an on-device pass over
the track's onset envelope (`WaveformExtractor.extractOnsetEnvelope` + pure `TempoEstimator`):
autocorrelation for the BPM (weighted by a ~120 BPM prior to fold half/double errors) and a
comb-filter for the downbeat phase. It **prefills** both, flagged as estimated for the user to
confirm, never auto-committing (rung 2 of ADR 0004). A **Click** toggle on the **speed bar**
(beside the BPM, its own teal — ADR 0027) plays a
**metronome** over the song that **follows the speed control** (50% → half-BPM, locked to the
slowed track): pure unit-tested `MetronomeSchedule` schedules each beat `delay = (beat − now) /
rate` ahead, played by a `ClickVoice` (a second player node on the same engine, straight to the
mixer so ticks aren't time-stretched, accented downbeat / plain beat). Enabled only when the
grid exists (BPM + the 1); it **never** alters the song's saved tempo and is silenced on pause /
screen exit (ADR 0026). Beside it sits a **repeat-the-song** toggle (ADR 0124) that wraps playback to
the top on the engine's natural-end callback instead of stopping — session state, wiped on exit,
and disabled while a loop is armed (that loop already repeats). Playback speed runs on **one axis**:
`TempoMath.minSpeed...maxSpeed` (0.25×–**1.5×**, lowered from 2.0× by ADR 0124) is quoted by the
waveform slider, the automator ramp, and the loop-run / song-play-along percent fields alike, with
stored speeds clamped on read (`TempoMath.clamped(speed:)`) rather than migrated; tapping the speed
readout opens **numeric entry** (pure `TempoMath.parse(speedEntry:)`), which *names* an out-of-range
value instead of silently rounding it. Pure gesture/zoom math in unit-tested `WaveformGesture`. The waveform shows the **whole** annotation library on its **borders** (off the
bars): markers as **purple inverted triangles** along the top, **all** saved loops
as **per-loop coloured lines** along the bottom; overlapping/nested loops **stack
into lanes** (pure, unit-tested `LoopLanes`) so overlap reads by position. Colour
now encodes loop **identity** (deterministic palette slot, pure unit-tested
`LoopColors`) with state carried by line weight, the active loop heavier — ADR 0023
(supersedes the colour-is-state rule of ADR 0018). The blue theme (blue bars anchored
on `#2a6796`) sits on the near-black background (ADR 0023). New loops are created **instantly** on confirm — auto-named
("Loop 3", via pure `AutoName`), activated, and **looping immediately** (no separate
play tap), no naming sheet — and **markers now drop the same way**: instantly,
auto-named ("Marker 3", same `AutoName`), no naming step, renamed later from the row
(ADR 0037, amending 0019's marker-naming exception); deleting a loop or marker offers an
**Undo** toast that restores it with its original identity (ADR 0019) — and since ADR 0125
that delete is **deferred** (the row hides; the object is destroyed when the window closes),
so Undo brings back the loop itself along with its journal, takes and routine links rather
than a rebuilt copy of its numbers. Holding the **Loops** or **Markers** panel header enters
**multi-select** (ADR 0125): the row's play glyph becomes a selection circle, the per-row
adjust/automator controls hide, and the header becomes a bar that bulk-deletes under one
undo, bulk-favourites (ADR 0119), and — in the collapse chevron's slot, reassigned only while
selecting — opens a **partial** editor for type · focus · tags across the selection
(fields the selection disagrees on read "Multiple" and stay unwritten; tags add, never
replace). Markers select the same way with delete only. The loop row's play glyph now also
carries the loop's ADR 0023 **identity colour** at all times — muted unless armed, so hue
reads as identity and saturation as state, with the green leading bar left as the sole
armed marker. Practice
opens on the **full song** — no loop is armed until you pick one — and leaving the
screen **wipes** the transient session knobs (active loop, speed, click, mode) while
persisted song data is left untouched (ADR 0029). Arming a loop is now **command-anchored** (ADR 0089):
a loop arms at its measured `commandTempo`, else 100% (`Loop.armingSpeed`) — **never** the tempo of the
loop you were just on — so switching to a loop with no command tempo resets to full tempo instead of
inheriting the previous loop's reduced rate (the tempo-bleed fix). This supersedes ADR 0040's arm-at-last-
practised for both arming sites (`activate`, the ◀◀/▶▶ `jump`); `Loop.lastPracticedSpeed` is still
recorded on leave (via the `activeLoopID` `didSet`) as the leave record, just no longer read to arm. The
**song** still resumes at its own last-practiced tempo on reopen (`Song.lastPracticedSpeed`, ADR 0044) via
the same choke point, which holds the invariant "no loop armed ⇒ `speed` is the song's tempo" (bank on
arm, restore on disarm) so a loop's speed never leaks in. The session still opens clean (no loop armed),
only the tempo is remembered.
State + handlers live in an `@Observable` `WaveformPracticeModel`
(ADR 0007), now bound to a **persisted `Song`** — loops/markers are SwiftData
`@Model`s that survive relaunches (ADR 0011). The practice screen is the **one
screen that rotates to landscape** (ADR 0042): the cockpit + loops/markers list are
extracted as `PracticeCockpit` / `PracticeReference`, stacked in portrait; in landscape
the waveform cockpit takes the full width (compact speed/transport bars, flexing waveform)
and the loops/markers list becomes a **slide-in drawer** (☰), gated to this screen by
`OrientationGate`. The old bottom **song-info panel was removed** — its facts live
in the song-details sheet (hold the title). The app opens to a **home hub** (`HomeView`, ADR 0044; planner review R1) — a greeting, a primary
**Start today's session** CTA (→ `PlannerView`, goal-adaptive), a "Jump back in" card
for the most-recently-practised song, a blue **Song library** nav strip, and — below the metronome —
a **Practice** card pushing the top-level **Practice
space** (`PracticeView`, ADR 0046 — a **hub** over two unit libraries: `ExerciseLibraryView`
(command drills) and `LoopLibraryView` (measured song **loops** by default, widened to every loop by
the **Show all loops** filter and narrowed to the flagged shelf by **Backing tracks only** — ADR
0138: the gate belongs to the *mode*, so the pure `LoopModeAccess` now answers it per mode (trainer
needs a command tempo, ear needs only playable audio, ADR 0135's improvise needs `isBackingTrack`
*and* playable audio) and each row offers exactly the modes that loop qualifies for — a trailing
**Ear** button on anything audible, **Improv** only on a flagged backing track, with the buttons, the
long-press menu and the pushed destination all built from `LoopModeAccess.modes(for:)` so a mode
added later reaches all three at once),
each a
row pushing its own list — each with a **sort menu + search** (`PracticeLibrarySort`, ADR 0056:
loops by Song · Name · Command · Mastery, exercises by Name · Command · Recently added; choice
persisted per library; the exercise **Command** key ranks on notes-per-minute — `command ×
notesPerBeat` via `NoteRate` — so tempos at different rhythms compare honestly, and every exercise
row shows the rhythm with its tempos ("Command 80 → 96 BPM · 16ths")). **Both lists are grouped and
collapsible** (v2 close-out Slice 5): exercises by template as before, loops **by song** — new in
that slice, replacing a flat list, via `PracticeLibrarySort.loopSections` (songs A→Z, detached loops
under "No song" last) — with every section folding from its header through the shared
`CollapsibleLibrarySection` (chevron · title · count), the same component the song library adopts.
The collapse persists per list in `@AppStorage` through the pure `LibrarySectionExpansion`: it
stores what's **collapsed**, so a section that appears later arrives open, and a live search forces
every section open so a query can't match rows inside a folded one. An exercise opens `ExerciseRunView`; a loop opens `LoopRunView` (Phase B)
— both owning their own engine. A **just-created** exercise opens the same way: `ExerciseLibraryView`
stages it on create and pushes its run screen once the create sheet is gone (skipped for a locked Pro
template, which stays badged in the list). **Every list row carries the same affordances**
(`.pocketRowActions`, `UI/PocketRowActions.swift`): hold for a menu reading *item actions →
Favourite → Delete*, swipe leading to favourite and trailing to delete, with a **Deleted X · Undo**
toast on every delete. The delete is **deferred**, not snapshot-restored: a screen-scoped
`RowDeletionCoordinator` (installed by `.pocketRowUndoHost()`, reached through the `\.rowDeletion`
environment seam) hides the row and only calls `context.delete` when the window closes — on timer
expiry, a second delete, screen disappearance or backgrounding — so Undo destroys nothing and a
song's cascade (loops, markers, journal) is never rebuilt by hand. The coordinator is **owned by the
screen** (`@State`) and passed into `.pocketRowUndoHost(_:)`, because a modifier applied inside a
view's `body` publishes its environment to that view's *descendants* only — the screen has to read
`isPending` itself to filter pending rows out of its list, empty state and filter menus, while the
environment seam is what the rows use. The trailing swipe's Delete is a plain `danger`-tinted button,
**not** `role: .destructive`, whose built-in row-removal animation would hide the row on a lie and
defeat Undo. Adopted by the exercise, routine,
loop and song libraries; every parameter is optional, so `LoopLibraryView` passes no delete (a loop
belongs to its song) and the song library no favourite (`Song` has no pin). Exercise rows gain
**Details** (the `ExerciseDetailSheet` previously only reachable from a run) and **Duplicate**;
routine rows gain **Play · Edit · Duplicate**. Duplication is pure model code
(`Core/Models/UnitDuplication.swift`): `CopyNaming` ("Spider copy" → "Spider copy 2"),
`Exercise.duplicated(named:)`, `Routine.duplicated(named:)` + `RoutineItem.copying(_:order:)` —
carrying shape and blocks, resetting mastery / lastPracticed / favourite / `presetSlug` (provenance is
never inherited — which closed the ADR 0112 fork bypass and still keeps a fork the player's own drill
rather than a copy of ours), and gated by the same `canAuthor` /
`canAuthorRoutine` checks as creation. On the
exercise run setup the tempos (working/command/reach) and the Steps granularity sit inside a
collapsible **Practice Settings** panel (`PracticeSettingsPanel`) below the title — collapsed by
default to a one-line tempo summary, mirroring the nested Steps disclosure. The
run staircase lights the live plateau as it climbs, tempos are typable as well as nudged, and the
routine takes reach / back-up steps beyond warm-up. The **reach is editable** (ADR 0075): it
defaults to the auto-derived goal above command but can be pinned to a custom target (nudged/typed in
Practice Settings, or in `ExerciseTempoSection`), with a **Reset to auto** affordance; a pin is stored
per unit (`Exercise.targetTempoOverride: Int?` / `Loop.targetSpeedOverride: Double?`, additive
optionals) and read through the effective `Exercise.reachTempo` / `Loop.targetSpeed`. A pin must stay
above command, so `promoteCommand` auto-clears it once command catches up (the vestigial
`Exercise.targetTempo` is retained un-removed for migration but no longer written). The
**back-off** tail (finishing below command on control, not the edge) is likewise controllable on both
exercises and loops: a **Back off** switch (`Exercise.includeBackoff` / `Loop.includeBackoff`, default
on) toggles it, and when on its floor is a pinnable override (`Exercise.backoffTempoOverride: Int?` BPM
/ `Loop.backoffSpeedOverride: Double?` ×, additive optionals mirroring the reach pin) with its own
**Reset to auto** — the `CommandRamp` takes an optional `backoffOverride` (percent for loops) and
otherwise derives the floor via `TempoStretch`. A new exercise picks a **time signature**
(`NewExerciseSheet`, default 4/4) — also editable on an existing exercise from the run-setup nav
bar — that drives the run click's accents + **count-in** length; a training run **counts you in**
before the climb (honoring the Settings toggle/length, ADR 0052). The running readout is just the
live BPM + beat dots — or, for an exercise with a **content template** (ADR 0065), the template's
own surface in place of the dots: `kind` selects the renderer (`.metronome` default → beat dots;
`.strumming` → `StrummingLaneView`, a down/up/mute/rest arrow lane (each slot independently
**accentable**) driven by the pure `StrumPattern` timing math; `.fretboard` → `FretboardView`,
notes walking a string × fret grid driven by the pure
`FretboardDrill` timing math — the shared surface for Scales/Picking/Legato/Fingerstyle/Warm-up,
ADR 0065 build 2; `.chords` → `ChordChangeView`, the current chord's diagram large with the next
previewed, swapping on the beat, driven by the pure `ChordProgression` timing over a shared
`ChordVoicing`/`ChordDiagramView` — triads are just three-note voicings, so they fold in here;
`.strumChords` → `StrumChordsView`, the chord surface stacked over the strum lane, both reading one
shared beat origin but each wrapping on its own independent cycle length — driven by a
`StrumChordSheet` pairing a `StrumPattern` and a `ChordProgression`, no reset coupling between them),
with an absent/undecodable payload falling back to the dots. The `Exercise` model stores its `CommandRamp`
recipe natively in `ramp*`/dwell/backoff/`rampReachSteps`/`rampBackoffSteps` fields, the
`automator* → ramp*` rename done data-preservingly via `@Attribute(originalName:)`. A loop trains
the **same** warm-up → dwell → reach → back-off `CommandRamp`, but in percent-of-original against
its time-stretched audio: `LoopRunModel` owns a `PracticeAudioEngine`, loops the region, and steps
the playback rate by elapsed seconds; the `×` reach derives from `TempoStretch.targetSpeed` (or a
pinned `targetSpeedOverride`, ADR 0075) and the staircase reuses `CommandRamp` via `LoopCommandRamp`'s
`×`→percent mapping), a metronome card, and a preview of your songs with
**See all** pushing the full **song library** (`LibraryView`), now one tap from the front
door rather than the root. Importing a DRM-free local/iCloud **audio file** takes a
security-scoped bookmark and extracts its real waveform (`WaveformExtractor`),
persisting a `Song` to practice, while an empty state offers import or a bundled
demo. Importing a **single** file (cleanly — `SongImportSummary.isSingleCleanImport`) pushes that
song's waveform straight away from either **+**; a batch lands in the library instead. **Holding a song card** → **Edit** opens a **song metadata sheet** (`SongEditSheet`)
(a context menu — swipe still offers a quick Delete, a tap opens the song for practice)
for title/artist/album/**genre** (canonicalised on write through `Labels.canonicalSingle`
and converged onto an existing library genre's spelling, ADR 0036)/year/**key** (a closed
`MusicalKey` picker — 12 roots × major/minor + Unknown — parsed from any legacy free text,
ADR 0036)/BPM/**downbeat**, lightweight **collections**,
a free-form **note**, and read-only **practice stats** (loops · markers ·
annotations) — the record we enrich to drive routines (ADR 0012). Collections are
canonicalised on write and **suggested from the ones already in the library** (so they
converge instead of fragmenting), and the library can be **filtered by collection** from a
toolbar **filter menu** (the funnel; intersection/AND) — ADR 0033. The library toolbar also
**names the current sort category** (e.g. "↑ Title") and lets you **flip ascending/descending**
(ADR 0035). The same `[String]` machinery (the shared
`Labels` canonicaliser) now backs loop-level **Tags** (ADR 0034). A song's **Mastery** is no longer stored — it is **derived**
as the rounded average of its **rated** loops' `mastery` (`MasteryRollup`, pure/unit-tested;
unrated loops are skipped, ADR 0039), shown as
stars and as a library group with an **Unrated** bucket for songs with no rated loops; the song also
records `lastPracticed` — **stamped on practice-screen entry** (ADR 0044) — for "recently
practised" ordering (home hub + library) and the planner (ADR 0036). Each loop carries a
per-loop **automator** (the "A" control on its row): a speed-trainer ramp — start % →
target % over N steps, a few loops each — that climbs, descends, or sits level, runs a
**fixed number of passes and then stops** (Set ramp also starts it playing), driven by the
engine counting loop wraps (ADR 0013). A loop row is **glanceable** (ADR 0039): under the name it shows the time range plus —
**only when set** — the loop's **mastery** (dots) and **command tempo** (a percent badge,
the headline achievement), so the loops list reads as a practice dashboard and an untouched
loop never shows a fake rating. Loop rows carry no edit pencil — **press and
hold** a row (with a haptic) to open the edit sheet for rename / range / colour /
delete (ADR 0028); the colour row pins a loop's identity hue — Auto, a preset, or a
custom colour wheel (low-contrast colours get an advisory hint) — or leaves it
automatic (ADR 0031). The edit sheet's **Practice** section carries the loop's
structured fields (ADR 0036), each with an explicit **unset** state (Optional, `nil` =
never set — ADR 0039, so a default never reads as a real rating; migrates pre-0039 loops to
`nil` for free): **Mastery** (0–5 dot rating, the source the song rolls up from; tap the
lowest filled dot down to clear back to *Unrated*), **Focus** (Backburner / Active /
Sharpening intent, stored 1–3, now a menu with a *Not set* option), **Type** (a closed
`LoopType` — Lick / Riff / Chords / Passage, single-select; Passage is the composite for a
loop spanning more than one), and **Command tempo** (the fastest tempo you own the loop at,
as a % of original; a **Set** button until measured — seeded from the loop's practice
speed — and a **Clear** back to unset) — the structured practice signal the planner reads.
Once a command tempo is set the Practice section also shows a **Practice now** button (ADR 0082) that
commits the sheet's edits and launches the loop's `LoopRunView` full-screen over the waveform,
returning here on exit — the same command-tempo gate that surfaces a loop into Practice → Loops.
Percent display + the `nil → "—"` fallback live in the pure `LoopProgressFormat`. A **Tags** section (ADR 0034)
adds the loop's open descriptive axis (`Loop.tags: [String]`) — the loop analogue of
song collections, canonicalised on write and **suggested from tags already used on any
loop** (cross-loop `@Query`); the cross-song filter-by-tag payoff is deferred to its first
consumer (the planner). Each loop also has a **practice journal** (ADR 0038): a dated log of
`JournalEntry` `@Model`s. **Authoring lives on the Practice run screens** (ADR 0058) — an inline
**Journal** section in `LoopRunView` / `ExerciseRunView` (a `JournalPreviewSection`: New-entry CTA
+ the latest 3 entries + See-all) opens the shared `JournalSheet` to add / edit / delete entries,
snapshotting the unit's context at that moment (the truthful place to write is right after a run).
The inline preview replaced an earlier nav-bar book button, which crowded the exercise
time-signature control. **ADR 0142 removes the "post-run, standalone" limit** that gate encoded: every
run screen carries a nav-bar ✎ (`QuickJournalButton`) opening the compact **`QuickJournalSheet`** —
one field + `EntryKindChipRow` + Save, medium detent — **while running and inside a routine**, where
the review bar is gated out; it writes through the same `JournalWriter.add` and **never touches the
transport** (the audio keeps playing behind it). Where a run screen has genuine room the field is
inline instead: `JournalNoteComposer` (the ex-`LoopModeNoteSection`, now taking a `JournalOwner` and
rendering as a Form section *or* a card) sits on the **freeform block**, under the **loop trainer's
live readout**, and on ear training / improvise as before. Deliberately not on the exercise run's
board surfaces, which have no space. Every entry **snapshots the owner's achievement at creation** —
copied, not referenced, so it stays truthful as the unit improves; the snapshot and timestamp are
immutable, only `text` and a typed **kind** (🎯 Goal / ⚡️ Breakthrough / 🧗 Struggle / 📝 Note /
🎬 Session — an `EntryKind`, primitive-backed like `LoopType`) are editable. Entries group under
day headers (`JournalGrouping`, pure), newest first. The loop-journal also lives in the loop's
**settings sheet** (a **Journal** row, ADR 0067), not on the loop row — the row's second control
became a one-tap **fine-adjust** button instead — and it is now **authorable** there too (ADR 0088,
reversing 0058's waveform read-only): the same `JournalSheet` + `JournalWriter` path, so a song loop
can be journalled without launching a run. The loop settings sheet also carries a **Train your ear**
button (ADR 0104) — ear training as "the loops, re-surfaced", an *away-from-the-guitar* exercise: an
`EarTrainingSheet` that plays the loop's own audio cycling continuously (via a `ContinuousLoopPlayer`
wrapping a `LoopRunModel`, no auto-stop — distinct from the routine-preview `LoopAudioPreviewPlayer`;
opening it **pauses the waveform** through `onOpenNestedAudio` → `pauseForNestedAudio`, the same
double-audio guard "Practice now" uses, so the transport reads Play on return) so
you **hum or sing it back**, with a live **−/+ tempo control** (25–150% of original, `LoopRunModel.
setAuditionPercent` pushing the rate mid-listen) to slow the phrase down. The loop's identity (name +
song prominent, type/tempo/range caption) is shown up top — no reveal toggle; "what you hear" notes save
through the same `JournalWriter` path tagged 👂 (`EntryKind.ear`). Self-judged, no score (ADR 0094
T2b/T3); real loop audio, not the Hear synth (ADR 0097). A sibling **Improvise** button (ADR 0135 Slice 1) opens `ImproviseSheet` on the same terms — ungated by command tempo, on **every** loop (the flag governs *resurfacing*, not permission), pausing the waveform through the same `onOpenNestedAudio` seam — running the loop as a **backing track**: continuous playback at one live-adjustable tempo, seeded from the loop's `armingSpeed` (command tempo else **full** tempo, not the ear seed — a section drawn at 70% to pick a lick off it is a poor jam tempo), no ramp and no rep clock (a song block's rule, not the trainer's), with "what you played" notes tagged 🎸 (`EntryKind.improvise`). **Nothing listens** (ADR 0070/0094): no mic, no analysis, no verdict. `ImproviseView` and `EarTrainingView` are the same three sections (identity header · `ContinuousLoopControls` from `LoopModeSections` · the shared `JournalNoteComposer`), differing only in copy and the note's kind — extracted when the second mode arrived rather than copied. The entry's owner is **polymorphic**
(ADR 0058/0143): `JournalEntry` relates to a `Loop` **or** an `Exercise` (exactly one) via
`JournalOwner` — **or to neither**, for an ADR-0143 **session** entry owned by a routine sitting
through loose id copies (`routineUID` + `routineNameAtEntry` + `practisedUnitsRaw`, all additive
optionals with no declaration default) rather than a third relationship, so the reflection outlives
the routine and every unit in it (`PracticeRun`'s precedent); which owner an entry has is read from
the **single computed `ownerKind`** discriminator (`.exercise`/`.loop`/`.session`/`.orphan`), never
from a hand-written `entry.exercise != nil` — that test was only correct while there were exactly two
owners, and would have rendered every session entry as a loop. So exercises get a journal too — an exercise entry snapshots its command in
**absolute BPM** (`commandBpmAtEntry`) **and the rhythm that BPM was measured in**
(`commandNotesPerBeatAtEntry`, ADR 0121 — without it an old entry's "80 BPM" stops being readable the
moment the drill's rhythm moves; entries written before it show a bare BPM rather than being
back-stamped, since the snapshot is immutable) and has no mastery, kept a distinct field from the
loop's song-fraction `commandTempoAtEntry` so a BPM is never stored as a fraction; the shared
`JournalWriter` builds each via the `forLoop`/`forExercise` factories. Songs get free-text
**notes** rather than a journal, and markers get neither.
Those song notes (`Song.comment`) live in a **Notes** section directly under the
title/artist/album header in the **song details sheet** — **editable inline behind a
pencil affordance**: tap it to edit, an **Update** button (disabled until the draft
changes) commits with a brief "Saved" confirmation; the rest of the sheet stays
read-first. The song-scope half of the notes/journal feature (ADR 0038).

⚠️ **The rest of this paragraph is historical.** The **metronome** shipped as its
own screen (ADR 0043) and the **planner** shipped in V2 (ADRs 0072/0073/0074) —
neither is "next". Filename suggestions and **collections as real playlists**
remain genuinely unbuilt; collections are still `[String]` tags, with the
promotion trigger recorded in ADR 0036. Kept for the reasoning it records:
the planner's **selection** (goals → required skills from a **technique taxonomy**
(`docs/practice-techniques.md`) → candidate items, *prioritised, not balanced*; ADR 0015)
and its **ordering/time-boxing** are grounded in practice science (spaced repetition +
serial-position effect + diminishing returns; ADR 0014); a **clean-before-fast** advance
gate for the speed-trainer is recorded for a later automator slice (ADR 0016).
Verified pure logic: `TempoMath`, `TempoPeaks`, `TempoEstimator`, `SongRef`, `AudioMath`, `WaveformGesture`, `BeatGrid`, `MetronomeBeats`, `MetronomeGrid`, `TempoMarking`, `TempoSliderScale`, `TimeSignature`, `MetronomeAutomator`, `TempoStretch`, `CommandRamp`, `LoopLanes`, `AutoName`, `Song`, `AutomatorConfig`, `EntryKind`, `JournalGrouping`, `MasteryRollup`, `LoopProgressFormat`, `PracticeStats`, `GlossaryTerm`, `ClickTimbre`, `PitchDetector`, `TunerReading`, `InstrumentTuning`, `TunerSmoother`.
See `CHANGELOG.md` for the full history.