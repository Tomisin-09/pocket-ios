<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Bundle keys. Previously injected by XcodeGen's `info:` generation; that
	     block was removed (it overwrote this file and dropped the keys below),
	     so they live here. $(…) resolve from project.yml build settings. -->
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- Apple Music library access is browse/metadata only in V1. This string is
	     surfaced verbatim in the system permission prompt; vague strings cause
	     App Store rejection. -->
	<key>NSAppleMusicUsageDescription</key>
	<string>Pocket reads your music library so you can pick songs to practise and see which ones you've already added loops and notes to.</string>

	<!-- Playback continues with the screen off during practice. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>

	<!-- NOTE: no NSMicrophoneUsageDescription. No V1 feature uses the mic
	     (the pedal modeller is parked; the metronome is output-only). Do not
	     add the key until a feature needs it. -->

	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>